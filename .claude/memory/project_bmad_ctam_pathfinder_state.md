---
name: project-bmad-ctam-pathfinder-state
description: Where the CTAM Pathfinder BMAD workflow stands after SCP 2026-08-24b (ctam-jomockapi onboarding + Phase 1-10+ renumbering)
metadata: 
  node_type: memory
  type: project
  originSessionId: fbd0b611-5a14-4dd5-baa5-5b011a548714
---

The repo's `_bmad-output/planning-artifacts/` tracks the **CTAM Pathfinder** programme (HMCTS judicial scheduling replacement). Sprint Change Proposal 2026-06-10 pivoted MVP wave 1 to the **SSCS jurisdiction** (replacing GAPS), retracted the Phase 0 ETL (reference data now ingested from JOH eLinks API + MRD weekly Excel), introduced two user populations (JOH via `jo_people` personnel number; admin staff via `auth_staff_identities` CTAM UUID), made jurisdiction a first-class hierarchical dimension, renamed `ctam-judge` → `ctam-joh`, and renumbered FR58–FR61 → FR57–FR60 (60 FRs total).

**Done (2026-06-11):** architecture pack fully amended to v3.0 — `architecture.md`, `architecture-summary.md`, all `architecture/` shards, sequence diagrams (incl. rename to `joh-onboarding-and-sitting-generation.*`), system-context diagram regenerated. Four architecture decisions taken with Ramnish: full rename now; in-process `@Scheduled` eLinks sync inside `ctam-reference-data`; MRD via Azure Blob drop + scheduled pick-up; CTAM-assigned UUID for staff identity.

**Done (2026-06-11, epics):** epics pack restructured per SCP — Phase 0 now 12 stories. Ramnish directed the eLinks/MRD ingestion stories into **Epic 0.1** (Stories 0.1.3/0.1.4 — sign-in depends on `jo_people`); Epic 0.2 renamed `epic-0.2-reference-data-read-only.md` (2 stories: tier-(b) tables/runbook + jurisdiction-filtered read API); Epic 0.3 renamed `epic-0.3-user-populations-bootstrapped.md` (1 story: seeds + bootstrap-verification job + runbook). requirements-inventory (FR1–FR60, AR46–AR49 now ingestion ARs, new AR52), fr-coverage-map, framework.md (incl. fixing never-applied D10 admin-UI area) all rewritten. 2026-05-15 validation report marked superseded.

**Standing constraint (2026-06-11, architecture v3.2): table naming** — every CTAM-owned table carries the `ctam_` prefix (`ctam_bookings`, `ctam_auth_users`, `ctam_regions`, `ctam_configuration_values`); upstream tier-(a) keep source prefixes (`jo_`, `mrd_`); no `_overlays` suffix (Ramnish's rule — `ctam_joh_ticket`, `ctam_joh_location`); `ctam_sync_status` (was `jo_sync_status`); `mock_*` dev-only tables exempt.

**Standing constraint (2026-06-11, architecture v3.1 / AR53):** HMCTS uses **Terraform** for all infra provisioning — and Ramnish's rule: *Terraform lives in the first repo that needs the resource; infra cannot live separate from the application that needs it* (MRD storage → `ctam-reference-data`; Static Web App → `ctam-ui`). **Shared estate relocated `ctam-authorisation` → `ctam-reference-data` on 2026-06-17** (decision #12 — see below), since `ctam-reference-data` is now the first service scaffolded. Apply the first-consumer rule to any future infra story.

**Done (2026-06-17, SCP 2026-06-17 / architecture decision #12 — integrations-first carve-out):** Ran `bmad-correct-course`. Approved change: Phase 0 is re-sequenced **integrations-first** — the JOH eLinks + MRD ingestion becomes the new **first epic**, decoupled from the auth/UI slice; `ctam-reference-data` is the first service scaffolded. **Repository question resolved: NO `ctam-integrations` repo** — ingestion stays in-process inside `ctam-reference-data` (reaffirms decisions #9/#10) because tier-(a) tables are single-writer-owned by `ctam_reference_data` (AR49) and a second deployable would break that invariant + reopen the G7 service-auth gap. **Key finding:** ingestion decouples cleanly from auth, but the **jurisdiction-filtered read API does not** (needs `JWTFilter` + `authz/check` for the jurisdiction it filters on), so it lands *after* the auth epic — new Phase 0 order: 0.1 ingestion → 0.2 auth+UI → 0.3 read API → 0.4 bootstrap → 0.5 notification (4 epics → 5; 12 stories preserved, no scope change; no PRD change). **Architecture pack applied:** `architecture.md` (decision #12 + Implementation Sequence), `changelog.md` (v3.4), `repo-structure.md` + `repository-strategy.md` (shared-estate relocation), `requirements-inventory.md` (AR53/AR23/AR27 first-consumer flip).

**Done (2026-06-17, epics restructure EXECUTED):** Phase 0 epic pack restructured 4 → 5 epics, ingestion-first. New files: `epic-0.1-upstream-reference-data-ingested.md` (4 stories: scaffold+estate / tier-(a) tables / eLinks sync / MRD), `epic-0.2-user-authenticates.md` (5: auth scaffold consuming estate / mock-auth / authz / ctam-ui / sign-in), `epic-0.3-reference-data-read-only-api.md` (2), `epic-0.4-user-populations-bootstrapped.md` (1), `epic-0.5-system-dispatches-emails.md` (2). **14 stories total** (was 12; +2 from splitting the old combined ctam-reference-data scaffold/tables/sync story — no scope added). The 4 old epic files (`epic-0.1-user-authenticates.md`, `epic-0.2-reference-data-read-only.md`, `epic-0.3-user-populations-bootstrapped.md`, `epic-0.4-system-dispatches-emails.md`) left as **redirect stubs** (not deleted — Ramnish to confirm deletion for a clean folder). Connective files updated: `phase-0/index.md`, `epics/index.md`, `fr-coverage-map.md`, `framework.md`. **Drift fixed during the re-run:** notification table `notification_delivery_log` → canonical `ctam_notification_dispatches` (data-tables.md v3.2). Verified: no dead story-number refs in bodies, no stale filename links except the historical v3.1 changelog entry (left as point-in-time record). **Next:** [IR] SSCS-cohort readiness check against the restructured Phase 0 → [SP] sprint planning (Epic 0.1 first sprint).

**Done (2026-06-17, PRD re-validation):** Ran `bmad-validate-prd` (12 steps; report `prd-validation-report-2026-06-17.md`). 0 blocking issues; BMAD Standard, 7/7 principles. Found + fixed (via "Fix Simpler Items") three residual SSCS-pivot staleness items in `prd.md`: (1) Domain §Technical Constraints "migrated from APEX in Phase 0" → bootstrap model (no migration); (2) API Backend section legacy "JI"/"Judge" → "CTAM Pathfinder"/"JOH (ctam-joh)" + Reference Data "admin-gated POST/PUT" → read-only (D10/D11); (3) "five journeys" → "six". **PRD now 5/5, consistent with D1–D12 and the current architecture/epics packs.** The integrations-first restructure did not touch the PRD. Docs site rebuilt (validation report added to "Product" nav group).

**Done (2026-06-17, [IR] implementation readiness):** Ran `bmad-check-implementation-readiness` (SSCS-cohort, integrations-first Phase 0; report `implementation-readiness-report-2026-06-17.md`, supersedes the four 2026-05-xx reports). **Verdict: READY for Phase 0 sprint planning — 0 critical, 2 major, 3 minor, 1 accepted UX gap.** Phase 0 FR coverage 100% (13 FR + NFR24 across 5 epics/14 stories); FR10–60 framework-mapped (storying pending by design). **2 conditions to handle at SP:** (1) auth seed-vs-demo forward dependency — Epic 0.2's sign-in E2E (0.2.5) needs Epic 0.4's seeds; recommend Epic 0.2 carry minimal inline auth fixtures while Epic 0.4 owns full seed+verification+runbook (pre-existing, not from the restructure); (2) JOH eLinks API contract unconfirmed (gaps.md G8.1) — Epic 0.1 buildable vs WireMock stub now, contract gates production cutover only → track as #1 external risk. Minor: split large Story 0.1.1 (scaffold vs estate); confirm ctam_configuration_values baseline is a discrete step; delete the 4 superseded phase-0 stub files. **Next: [SP] `bmad-sprint-planning` (Phase 0, lead with Epic 0.1).**

**Done (2026-08-07) — SCP 2026-08-07 / D13 supersedes D11 for wave ordering: wave 1 = Employment Tribunals (ET), wave 2 = SSCS, waves 3+ = Courts per region.** Documentation cascade EXECUTED across the planning-artifact set (architecture v4.0). Three new gaps, all wave-1 blockers: **G8.4** ET scheduling incumbent unidentified (recorded everywhere as the placeholder token `[ET-INCUMBENT-TBD]` — resolving it is a single mechanical sweep); **G8.5** ET role taxonomy/as-is pack (every ET role name in the artifacts is *provisional*); **G8.6** JFEPS applicability to ET unverified (SSCS was explicitly verified 2026-06-11; that check does NOT transfer). G8.1 promoted to wave-1 blocker (eLinks `jo_jurisdictions` must carry ET). Architecture, epics, data model, FR/NFR set and the 6 Phase-0 epics / 19 stories are **structurally unchanged** — D8 made jurisdiction upstream-sourced, so retargeting the pilot was a data-and-rollout change.

**Done (2026-08-13, SCP 2026-08-13 / D14):** Programme renamed **RAM → CTAM** (Court and Tribunals Availability Management). Naming-only cascade, zero semantic content: product codename `RAM Pathfinder` → `CTAM Pathfinder`; the 17 repo names `ram-*` → `ctam-*` (16 documented + the not-yet-onboarded `ctam-jomockapi`) (this repo `ram-analysis` → `ctam-analysis`, already renamed on GitHub); tier-(b) table prefix `ram_` → `ctam_`; Java root package `uk.gov.hmcts.ram` → `uk.gov.hmcts.ctam`; API-spec artefact coordinates, shared DB schema, DNS hostnames and adjectival prose (`RAM-owned`/`RAM-assigned`/etc.) all follow — ~2,780 occurrences across 88 files plus 80 regenerated `docs/` files. Deliberately **not** renamed: upstream tier-(a) `jo_`/`mrd_` prefixes, dev-only `mock_` prefix. **Convention override (scoped to this change only):** dated reports, prior SCPs and pre-v4.1 changelog entries were rewritten in place rather than preserved verbatim, so the artifact set is uniformly searchable under one name — git holds the point-in-time record. Architecture → v4.1. No code impact (implementation not started).

**Done (2026-08-19, SCP 2026-08-19):** Adopted a binding agent-rules pack for the Java execution units — evidence-based TDD (no `src/main/**` edit without a test that ran red on an assertion first), hard modularity limits (method ≤30 lines, file ≤300, cyclomatic ≤8, etc.), a cite-or-ask uncertainty protocol, and one pre-handoff quality gate (`./scripts/verify.sh`) — authored on the `ctam-architecture` context bus as `agent-rules/`. Amended the behaviour-coverage convention with build-failing floors: JaCoCo ≥85% line / ≥75% branch, PIT mutation ≥70% on `service`+`domain`. No FR/NFR/epic/story/sequencing change. Architecture → v4.2.

**Done (2026-08-19, SCP 2026-08-19b):** Fixed a self-contradiction in `delivery-operating-model.md` — it specified a bespoke story-packet layout while separately mapping dispatch to `bmad-create-story`. The packet is now defined as **BMad template + CTAM polyrepo fields**, with a canonical template on the bus, a committed BMad customization, and a validator. Also surfaced that `bmad-create-story` cannot run without `sprint-status.yaml`, which `bmad-sprint-planning` had never actually been run to produce. Architecture → v4.3.

**Done (2026-08-19, SCP 2026-08-19c):** The human gate moved from **every commit to the pull request**. Claude sessions may now branch, commit and push a feature branch; a `PreToolUse` hook (`.claude/hooks/block-git-writes.sh`) still denies writes to a protected branch, force-pushes, tags, work-discarding operations, and all `gh`/`hub`. Architecture → v4.4.

**Done (2026-08-19, SCP 2026-08-19d):** Retired the bespoke per-epic traceability ledger and `delivery/dispatch-graph.yaml` — story/epic status now lives in BMad's own `sprint-status.yaml`; the two facts BMad doesn't model (`repo`, `depends_on`) moved into each epic's frontmatter. Multi-user coordination becomes one-dispatcher-by-convention, backed by `scripts/dispatch-preflight.sh`'s read-only branch-existence check. The arch-baseline graph node was promoted to a first-class epic (Epic 0.6, later renumbered **Epic 1.6** by SCP 2026-08-24) so BMad tracks it. Architecture → v4.5.

**Done (2026-08-20, SCP 2026-08-20):** Dispatch now creates `story/{story-id}` in the target repo **at dispatch time** (not on first commit) and pushes it immediately; the implementing session continues on that same branch through to the PR — closes the window in which a dispatched story had no visible claim, and gives the pre-dispatch check a branch that actually exists. Architecture → v4.6.

**Done (2026-08-20, changelog v4.7 — no separate SCP file, recorded from the `bad68ee` tooling commit):** Hardened `bmad-create-story`'s CTAM override after finding six silent failures. Load-bearing one: the skill's default epics glob (`{planning_artifacts}/*epic*/*.md`) missed CTAM's actual epic files entirely (they sit one level deeper, at `epics/phase-<n>/`), so `{epics_content}` could come back empty and acceptance criteria would be **invented** rather than carried verbatim from the epic — now bound explicitly with a HALT on a glob matching none or more than one file. Also: architecture was being distilled from the control plane's **live** copy instead of the pinned `bus_version` (now reads the tag directly via `git -C ../ctam-architecture show <tag>:...`); this surfaced that `arch-v1.0` lacks the story-packet template, the PR-as-gate policy and one-branch-per-story — so **`arch-v1.1` was tagged 2026-08-20 and is now the minimum `bus_version` for any packet** (any service repo still pinned at `arch-v1.0` needs an ordinary submodule-bump commit before its first dispatch). Plus: epic-status guards now also run on direct story invocation (not just auto-discovery), an unbound `{story_file_path}` fixed, the invented `dispatch/` branch prefix corrected to the sanctioned `chore/dispatch-<id>`, `validate-story-packet.sh`'s section check flipped from an eight-name blacklist to a five-heading allowlist, and `dispatch-preflight.sh` gained a fourth check enforcing intra-epic story order. Tooling/rules hardening only — no scope or architecture change.

**Done (2026-08-21, SCP 2026-08-21):** Retired the committed `bmad-create-story` team customization ahead of stock BMad 6/7 native polyrepo/multi-user support, and recorded the nine CTAM deviations from out-of-the-box BMad in a deviation register.

**Done (2026-08-24, SCP 2026-08-24):** Renumbered the whole phase sequence **+1** (Phase 0 Foundations → **Phase 1**, … Phase 9+ Wave Rollout → **Phase 10+**) at direct stakeholder request, since "Phase 1" was already taken by JOH elsewhere in the corpus. The already-decomposed epic pack renumbered in lockstep (`epics/phase-0/` → `epics/phase-1/`, `epic-0.X` → `epic-1.X`, `Story 0.X.Y` → `Story 1.X.Y`). Dated historical reports and the PRD's own D1–D14 decision log / editHistory entries left untouched (add, don't rewrite convention); new **D15** + changelog v4.9 record the change, including the accepted numbering seam on Epic 1.6 (its story 0.6.1 was already tagged `arch-v1.0` in `ctam-architecture` under the old numbering).

**Done (2026-08-24, SCP 2026-08-24b):** Onboarded **`ctam-jomockapi`** — an already-built local Node.js/Express mock of the upstream Judiciary E-links People API (v5), built from the real swagger doc + example payloads — as a **17th repo**, classified dev/integration-only like `ctam-mock-auth` (never deployed to production, so every existing "11 services" statement is unaffected; only repo-**count** mentions moved 16→17). Gives `ctam-reference-data`'s Epic 1.1 ingestion sync a realistic contract to build and test against ahead of the real eLinks API contract being confirmed (gaps.md **G8.1**, partially de-risked, not closed). Added a new **Phase 0** (`epics/phase-0/`) holding **Epic 0.0** (2 stories) — explicitly **not part of the product's own phase sequence** (it precedes Phase 1 numerically only because it's a dev prerequisite). Architecture → v5.0.

**CURRENT (2026-08-24):** Architecture pack at **v5.0**, **17-repo polyrepo**, Phase 0 (dev prerequisite) + Phase 1–10+ (product) fully renumbered and cross-referenced across `_bmad-output/planning-artifacts/` and `docs/`. `sprint-status.yaml` is live and current: **Epic 1.6 in-progress** (story 1-6-1 `done` — `ctam-architecture` context bus published and tagged `arch-v1.0`); Epic 0.0 and all other Phase 1 epics `backlog`. **Dispatch note: `arch-v1.0` is no longer sufficient** — changelog v4.7 tagged **`arch-v1.1`** on 2026-08-20 as the minimum `bus_version` (adds the story-packet template, PR-as-gate policy, one-branch-per-story); any story dispatched now must pin `arch-v1.1` or later. Dispatch discipline: `story/{story-id}` branch created at dispatch time; the pull request is the human gate; one-dispatcher-at-a-time enforced by `scripts/dispatch-preflight.sh`.

**Next:** Dispatch the next Phase 0/Phase 1 epic via `bmad-create-story`/`bmad-build` (Epic 0.0 JOH mock APIs is a dev prerequisite; Epic 1.0 platform estate is the first product epic). Programme-level gaps still open since 2026-08-07: **G8.4** (ET scheduling incumbent unidentified — `[ET-INCUMBENT-TBD]`), **G8.5** (ET JOH role taxonomy provisional pending the ET as-is analysis pack), **G8.6** (JFEPS/Liberata applicability to ET unverified). The **ET as-is analysis pack** (gates G8.4/G8.5/PRD Journey 1, under `docs/architecture/asis/`) and the **ET business-case re-justification** remain the long poles; the SSCS as-is pack still defers to wave 2.
