---
story_id: 1.9.1
epic: epic-1.9-context-bus-and-shared-baseline
repo: ctam-architecture
bus_version: arch-v1.1
frs: []
nfrs: []
depends_on_stories: []
sprint_status_key: 1-9-1-publish-ctam-architecture-as-the-official-version-tagged-architecture-package
---

# Story 1.9.1: Publish `ctam-architecture` as the official, version-tagged architecture package

Status: done

## Story

As a **platform engineer**,
I want the architecture documents and the agent working-rules pack published together in the `ctam-architecture` repository and tagged as a release,
so that **every service team can pin exactly one version of the shared architecture**, and only move to a newer version through a deliberate, visible update — never silently.

## Acceptance Criteria

1. Given the official architecture documents live in this planning repository, when the publishing script is run, then the main architecture document, its summary, the product requirements document, and every supporting architecture section are copied into the `ctam-architecture` repository, and a note in that repository clearly states these files are a mirror and must never be hand-edited there, and the script itself makes no version-control changes (such as commits or tags) — it only copies files.
2. Given the published files plus the authored agent working-rules pack are ready on the main branch, when the release is tagged, then a proper, annotated release tag (`arch-v1.0`) exists on the `ctam-architecture` repository, and that tag is pushed and can be seen by anyone checking the repository's tags, and the tag is created by a person, not by an automated coding session — that's a deliberate human checkpoint.
3. Given a service repository needs the shared architecture, when it pulls in `ctam-architecture` as a linked sub-repository at the `arch-v1.0` tag, then both the core agent working-rules file and the shared naming-and-conventions document are present and readable inside it, and the exact version being used is written down in that service's own project instructions and in each piece of work's own tracking record.
4. Given a shared convention changes later on, when that change is published, then it comes out as a new, higher-numbered release tag together with a deliberate update in each service repository that adopts it — the shared package never silently changes what a service is already using.

## Tasks / Subtasks

- [x] Task 1 (AC: 1) — Run `scripts/publish-arch.sh` from the control-plane repo (`ctam-analysis`) to copy `architecture.md`, `architecture-summary.md`, `prd.md` and every `architecture/*.md` shard into `ctam-architecture`; confirm the script writes `architecture/PUBLISHED.md` (the mirror warning) and contains no `git commit` / `git tag` / `git push` call.
- [x] Task 2 (AC: 2) — Author the `agent-rules/` pack directly on the bus (it is not part of the mirrored copy), land it on `main`, and have a human — not an agent session — create and push the annotated `arch-v1.0` tag.
- [x] Task 3 (AC: 3) — Confirm `agent-rules/00-core.md` and `architecture/conventions.md` are present and readable in the tagged tree, and confirm the convention that the pinned bus version is recorded in each service's own `CLAUDE.md` / story packet `bus_version` field.
- [x] Task 4 (AC: 4) — Exercise the re-publish path for a later change: cut `arch-v1.1` as a new, strictly higher-numbered tag rather than moving or rewriting `arch-v1.0`.

## Dev Notes

### Context distilled from the bus

- `_arch/architecture/PUBLISHED.md`: the mirror warning ships with the published payload itself — "Every `.md` beside this file, plus `../architecture.md`, `../architecture-summary.md` and `../prd.md`, is a published mirror... To change any of it: change the canonical file [in `ctam-analysis`], then re-run `publish-arch.sh` and publish a new `arch-vN` tag." This is the exact evidence AC1's "a note ... clearly states these files are a mirror" asks for.
- `_arch/agent-rules/index.md` § "Publication status": states the architecture payload "is now published here... by `ctam-analysis/scripts/publish-arch.sh`" and that `agent-rules/` itself is **not** a mirror — it is authored directly on the bus. One sentence in that same section ("The `arch-v1.0` tag is not published yet") is stale leftover prose from before the tag was actually cut — the tags demonstrably exist (see Dev Agent Record). A reader should not take that single line as current status; it was not updated after tagging.
- `_arch/agent-rules/index.md` § "How these rules reach a service repo": defines the consuming shape (`_arch/` submodule pinned to an exact tag, `CLAUDE.md` generated from the enforcement template naming the pinned version) — this is what AC3's "written down in that service's own project instructions" cashes out to once a service repo exists.

### Rules that apply

- This story is a publish/tag action on documentation, not a Java service change, so the TDD-loop rules (`10-tdd.md`) do not bite here in their usual form.
- `_arch/agent-rules/00-core.md` R13 ("never write to a protected branch... never tag") is the rule that makes AC2's "created by a person, not an automated coding session" a hard requirement rather than a style preference — tagging is explicitly reserved to a human checkpoint programme-wide, matching this repo's own `CLAUDE.md` hard rule that `arch-vN` is a release action outside agent scope.
- R12 (no success claim without evidence) governs how this record is written: every claim below is backed by a command actually run or a file actually read, not asserted from memory.

### Project Structure Notes

- The publishing script lives in the control-plane repo, not this one: `ctam-analysis/scripts/publish-arch.sh`. It copies `_bmad-output/planning-artifacts/{architecture.md,architecture-summary.md,prd.md,architecture/*.md}` into the bus and writes `architecture/PUBLISHED.md` — nothing else, and no `diagrams/`, `sequence-diagrams/` or `architecture/analysis/` (deliberately deferred, per the script's own comments).
- `agent-rules/` in `ctam-architecture` is hand-authored on the bus and is not touched by `publish-arch.sh` — confirmed by reading the script (no `agent-rules` reference in it) and by `agent-rules/index.md`'s own statement that it is not a mirror.

### Out of scope / boundaries

- Publishing `diagrams/`, `sequence-diagrams/` or `architecture/analysis/` into the bus — deferred until a consumer actually needs them, per the epic's "Explicitly out of scope" list.
- A read-only mirror of any individual service's own API specification — deferred until the first service's API spec exists, per the same list.
- Creating or granting access to the `ctam_configuration_values` table — that is Story 1.9.2, not this one; this story only publishes documents and rules, it does not touch the database.

### Recorded deviations

- `bus_version` in this packet's frontmatter is recorded as `arch-v1.1`, not the `arch-v1.0` literal text quoted in AC2/AC3. This is not a departure from the story — AC4 is the acceptance criterion that *describes* a later higher-numbered tag superseding v1.0, and `arch-v1.1` (`2479a2dac5c9126cc60ea99aabd9a8265c4c6e7e`) is exactly that: a real, already-published second tag, verified in this session (see Dev Agent Record). Citing the currently-superseding tag rather than the historical first one is the correct reading of "pin exactly one version" for any packet written after v1.1 shipped.

### Definition of done

- `_arch/agent-rules/90-definition-of-done.md`'s Q-items are written for a Java service TDD story (red/green test evidence, `./scripts/verify.sh`, JaCoCo/PIT thresholds). None of that machinery exists for this story — there is no Java code path. Waived in full: every Q-item tied to test execution, coverage, or mutation-testing gates. The substitute gate that actually applied was AC2's human-tagging checkpoint, which is outside any agent's authority to perform or to fake.

### References

- [Source: _arch/architecture/PUBLISHED.md]
- [Source: _arch/agent-rules/index.md § Publication status]
- [Source: _arch/agent-rules/index.md § How these rules reach a service repo]
- [Source: _arch/agent-rules/00-core.md R13]
- `ctam-analysis/scripts/publish-arch.sh` (control-plane repo; read in full this session)
- `git ls-remote --tags https://github.com/hmcts/ctam-architecture.git` (run this session) → `refs/tags/arch-v1.0` = `e4b6ea4c2f746cd48bf0675b2578453ae71b5616`, `refs/tags/arch-v1.1` = `2479a2dac5c9126cc60ea99aabd9a8265c4c6e7e`

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 — retrospective record: this packet documents a story whose delivery (publish + human tag) was already complete before this drafting session; the model above is the one that authored this record, not necessarily the one that ran the original publish.

### Debug Log References

None. This story is a publish/tag action, not a code build — there is no test-run log. The tag listing below is the operative evidence.

### Completion Notes List

- Ran `git ls-remote --tags https://github.com/hmcts/ctam-architecture.git` (read-only; no clone, no push) on 2026-08-21. Output: `refs/tags/arch-v1.0` → `e4b6ea4c2f746cd48bf0675b2578453ae71b5616` and `refs/tags/arch-v1.1` → `2479a2dac5c9126cc60ea99aabd9a8265c4c6e7e`, both present on the remote. This satisfies AC2 (a proper, pushed, checkable tag exists) and demonstrates AC4 already happened for real — `arch-v1.1` is a genuine higher-numbered follow-on tag, not a rewrite of `arch-v1.0`.
- Read `ctam-architecture/architecture/PUBLISHED.md` in the local read-only clone (checked out at `arch-v1.1`): it states the mirror-not-hand-edit rule required by AC1.
- Read `ctam-architecture/agent-rules/index.md` and `agent-rules/00-core.md`: both present and readable, satisfying AC3's "core agent working-rules file"; `architecture/conventions.md` is likewise present, satisfying AC3's "shared naming-and-conventions document".
- Read `ctam-analysis/scripts/publish-arch.sh` end to end: it only `cp`s files and writes `architecture/PUBLISHED.md`; it contains no `git commit`, `git tag`, or `git push` invocation — satisfying AC1's "the script itself makes no version-control changes."
- Cross-checked `_bmad-output/implementation-artifacts/sprint-status.yaml`: `1-9-1-publish-ctam-architecture-as-the-official-version-tagged-architecture-package: done`, consistent with this record.
- Not independently re-verifiable from this planning repo: which specific human ran `git tag`/`git push`, or the exact PR that landed the mirrored files on `ctam-architecture`'s `main` before tagging. The tags' existence and the mirror's content are the checkable evidence available here; the human-checkpoint act itself is attested by this repo's own `CLAUDE.md` hard rule (tagging is a human release action, never an agent one), not by a transcript available to this session.

### File List

No files were added, changed, or deleted by this drafting session — this packet is a completed-story record, not new work. Files that constitute the delivered story, all in the `ctam-architecture` repo (currently at tag `arch-v1.1`, superseding the first release `arch-v1.0`):

- `architecture.md`, `architecture-summary.md`, `prd.md` (mirrored from `ctam-analysis`)
- `architecture/*.md` shards, including `conventions.md`, `data-tables.md`, `delivery-operating-model.md`, `PUBLISHED.md`
- `agent-rules/` pack — `00-core.md` … `90-definition-of-done.md`, `index.md`, `enforcement/`, `templates/` (authored directly on the bus, not mirrored)

And, in the control-plane repo (`ctam-analysis`):

- `scripts/publish-arch.sh` (the repeatable publishing script the epic calls for)
