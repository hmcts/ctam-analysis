---
story_id: 1.9.2
epic: epic-1.9-context-bus-and-shared-baseline
repo: ctam-architecture
bus_version: arch-v1.1
frs: [FR8]
nfrs: []
depends_on_stories: [1.9.1, 1.0.3]
sprint_status_key: 1-9-2-shared-configuration-table-exists-with-read-only-access-for-every-service
---

> **DRAFT — NOT DISPATCHED.** Authored ahead of the real dispatch flow because this story's epic dependencies (Story 1.9.1 aside) are not yet fully `done`. `sprint-status.yaml` still shows this story as `backlog`. Before this is real: run `scripts/dispatch-preflight.sh <story-id>`, re-verify `bus_version` against the repo's actual state, then land it per delivery-operating-model.md's DISPATCH BRANCH step and flip `sprint-status.yaml` to `ready-for-dev` in the same change.

# Story 1.9.2: Shared configuration table exists, with read-only access for every service

Status: ready-for-dev

## Story

As a **service developer**,
I want one shared table of typed, programme-wide settings that my service can read but not change,
so that **settings used by several services — things like session-timeout warnings, batch schedules, and feature flags — live in exactly one place**, instead of being copied into every service or hard-coded.

## Acceptance Criteria

1. Given the shared database instance already exists, and the engineer adds the `ctam-architecture` repository's baseline database migration script, when that migration script is applied, then the `ctam_configuration_values` table exists in the shared database schema, and it holds typed settings — a key, a value, and the value's type — with a `uuid` primary key column named `id`, and `created_at` and `updated_at` timestamp columns that can never be left blank, following the shared naming conventions used across the programme, and no extra column is added beyond that simple shape without a clear, justified reason, and the table is written down as shared infrastructure with no single owning service in the programme's record of shared database tables.
2. Given the table exists, when database access is granted, then every service's database role can read from it, and no service's database role can insert, update, or delete rows in it — for now, changes are made only by a database administrator, and those access grants are written into the `ctam-architecture` repository's migration scripts rather than applied by hand, so they can't quietly drift or be forgotten.
3. Given a service needs to read a setting, when it does so, then it reads the value straight from the shared database using its normal data-access layer — there is no separate configuration service or client to call, and the value is always treated as a policy setting, never as a secret — actual secrets stay in the programme's secure secrets vault.
4. Given an automated integration test runs, when the baseline migration script is applied to a freshly created, empty test database, then the table and its access grants are created from nothing and checked automatically, proving the migration script itself works correctly rather than relying on a developer's own already-set-up database.

## Tasks / Subtasks

- [ ] Task 1 (AC: 4) — Write the failing integration test first: an `*IT.java` (Testcontainers, real PostgreSQL — no H2) that spins up an empty database, applies the `ctam-architecture` baseline Liquibase changelog, and asserts `ctam_configuration_values` does not yet exist before the changelog runs. Run it, capture the assertion failure.
  - [ ] Confirm the test fails for the right reason (table absent), not a setup error.
- [ ] Task 2 (AC: 1) — Add the baseline changelog (`db/changelog/NNN-ctam-configuration-values.sql`, registered in `db.changelog-master.yaml`) creating `ctam_configuration_values` with `id uuid` PK, a typed key/value/type shape, `created_at timestamptz NOT NULL`, `updated_at timestamptz NOT NULL`, and a `--rollback` clause. Re-run the Task 1 test to green.
- [ ] Task 3 (AC: 1) — Add `ctam_configuration_values` to the shared-infrastructure section of `data-tables.md` (or confirm the existing entry already covers it) so the ownership fitness function recognises it as owned by no single service.
- [ ] Task 4 (AC: 2) — In the same changelog set, add `GRANT SELECT ON ctam_configuration_values TO <each service DB role>` and explicitly withhold `INSERT`/`UPDATE`/`DELETE` from every role. Write a test asserting a non-admin role's write attempt is rejected by PostgreSQL, and a read succeeds.
- [ ] Task 5 (AC: 3) — Add a Dev Notes example (not production code — this is a per-consuming-service pattern, not a library) showing a plain repository-layer read of a key via the service's normal data-access layer, with no configuration client or service introduced; confirm no secret-shaped value is modelled in this table.
- [ ] Task 6 (AC: 4) — Confirm the full `*IT.java` suite runs the changelog against a freshly created, empty Testcontainers database end to end (schema + grants), and paste the gate output.

## Dev Notes

### Context distilled from the bus

- `_arch/architecture/data-tables.md` § "Shared infrastructure tables (no owning service) — 1 table": `ctam_configuration_values` is "Schema-managed by `ctam-architecture`'s Liquibase baseline changelog; SELECT-granted to every CTAM Pathfinder service DB role; writes are admin-or-Liquibase-only (no API)." and "Typed cross-service policy values (D1 — runtime policy keys that need to be visible to multiple services). Per-service configuration that is scoped to a single service uses Spring profiles + `application.yml` + Azure Key Vault, **not** this table." — this is the exact boundary AC3's "never a secret" and "no separate configuration service" clauses are drawing on.
- `_arch/architecture/data-tables.md` § "Table Ownership Summary": "Shared infrastructure: 1 table (`ctam_configuration_values`; no owning service)" — the record AC1's "written down ... with no single owning service" points at.
- `_arch/architecture/conventions.md` § "Naming Patterns": PKs are `id`, type `uuid`; columns `snake_case`; audit columns `created_at timestamptz NOT NULL`, `updated_at timestamptz NOT NULL` on every table (`created_by`/`updated_by` are post-MVP, D7 — do not add them speculatively per R6). `ctam_configuration_values` is listed by name as one of the `ctam_`-prefixed CTAM-owned tables.
- `_arch/agent-rules/40-data-and-liquibase.md` P1 (Liquibase DDL only, Flyway prohibited), P2 (applied changesets are immutable — fix forward only), P3 (one logical change per changeset, with a rollback), P4 (cross-service `GRANT`s live in the *owning* service's changelog — here, `ctam-architecture`'s own baseline, since this table has no owning service in the usual sense), P6 (every migration proved against real PostgreSQL via Testcontainers `*IT.java` — H2 prohibited), P16 (audit columns populated by the application via an injected `Clock`, never a DB trigger or default — though for this table, rows are written only by Liquibase seed/DBA SQL, not application code, which the story should decide explicitly rather than default silently; see Open point below).
- `_bmad-output/planning-artifacts/epics/fr-coverage-map.md` row FR8: "distributed (lands in Epic 1.1 Story 1.1.1 first) — Shared `ctam_configuration_values` Liquibase baseline changelog." Story 1.1.1 (Epic 1.1) scaffolds `ctam-reference-data` and establishes the *first* Liquibase changelog pattern any service follows; the coverage map's "first" landing is that scaffolding precedent, not the table itself. This story is where `ctam_configuration_values` is actually created, per the epic's own text ("a database migration script owned by the `ctam-architecture` repository") and per `data-tables.md`. Flagging this so `frs: [FR8]` on this packet isn't read as duplicating or overriding the fr-coverage-map's own routing — both are true: the pattern lands first in Epic 1.1, the table itself lands here.

### Rules that apply

- `_arch/agent-rules/00-core.md` — full R1–R14 loop applies: red test before any changelog/production code (R2), one behaviour per test (R3), cite-or-ask (R4), no unsanctioned surface area — no extra columns beyond key/value/type + audit (R6), nothing unfinished ships (R8), evidence-backed success claims only (R12).
- `_arch/agent-rules/40-data-and-liquibase.md` — read in full before writing the changelog; P1–P7 govern the migration itself, P16 governs the audit columns.
- `_arch/agent-rules/50-security-and-logging.md` — read before deciding what a "policy setting" is allowed to hold; this table must never carry anything secret-shaped (R11 territory) — that's the whole reason AC3 exists.

### Project Structure Notes

- Changelog file: `db/changelog/NNN-ctam-configuration-values.sql` in the `ctam-architecture` repo's own Liquibase folder, registered in `db.changelog-master.yaml`, per `_arch/agent-rules/40-data-and-liquibase.md` P1.
- No `domain/`, `service/`, or `controller/` package for this story — there is no owning service and no API (AC3, and the epic's own "Explicitly not in scope" list). Any example read shown in this packet is illustrative Dev Notes content for consuming services, not production code shipped by this story.
- Integration test: `*IT.java` under this repo's test source, using Testcontainers PostgreSQL per P6 — no H2.

### Out of scope / boundaries

- Any screen or API to write to these values — per the epic's "Explicitly not in scope", changes are made directly by a DBA following standard operating procedure; no write endpoint exists in any service.
- Configuration specific to a single service — stays in that service's own `application.yml` / Spring profiles / Azure Key Vault, never migrates into this shared table (`data-tables.md`'s own boundary statement).
- Publishing the architecture bus itself — that is Story 1.9.1, already done; this story only depends on it for the changelog landing in the tagged bus, not for re-publishing anything.

### Definition of done

- `_arch/agent-rules/90-definition-of-done.md` applies in full — this is an ordinary Liquibase-changelog story with a real `*IT.java` gate (P6). No Q-items are waived: JaCoCo/PIT thresholds, `./scripts/verify.sh`, and R2 red/green evidence all apply because this story does add a real schema artefact with a testable migration path, even though it has no owning service in the usual sense.
- Open point to resolve at implementation time, not guessed here (R5): whether `created_at`/`updated_at` on rows written by DBA-run SQL (rather than application code) are populated by the SQL itself at insert/update time, since P16's normal mechanism ("set by the application from the injected `Clock`") assumes an owning service's application code, which this table does not have. Record the answer under this section when the story is actually picked up.

### References

- [Source: _arch/architecture/data-tables.md § Shared infrastructure tables (no owning service) — 1 table]
- [Source: _arch/architecture/data-tables.md § Table Ownership Summary]
- [Source: _arch/architecture/conventions.md § Naming Patterns]
- [Source: _arch/agent-rules/40-data-and-liquibase.md P1–P7, P16]
- [Source: _arch/agent-rules/00-core.md R1–R14]
- [Source: _bmad-output/planning-artifacts/epics/fr-coverage-map.md row FR8]
- [Source: _bmad-output/planning-artifacts/epics/phase-1/epic-1.0-platform-estate-provisioned.md Story 1.0.3 (the shared database instance this story's AC1 presupposes)]

## Dev Agent Record

### Agent Model Used

Not yet dispatched — no dev agent has picked up this story. This packet is a pre-dispatch draft (see the DRAFT banner).

### Debug Log References

None yet.

### Completion Notes List

None yet — to be filled in by the dev agent per R2/R12 when this story is actually executed.

### File List

None yet.
