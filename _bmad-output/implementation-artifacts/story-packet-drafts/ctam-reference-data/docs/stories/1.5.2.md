---
story_id: 1.5.2
epic: epic-1.5-joh-reference-data-read-api
repo: ctam-reference-data
bus_version: arch-v1.1
frs: [FR7, FR58]
nfrs: []
depends_on_stories: [1.5.1, 1.2.1, 1.4.3]
sprint_status_key: 1-5-2-reference-data-read-only-rest-api-with-jurisdiction-filtering-versioning-openapi-rfc-9457-errors
---

> **DRAFT — NOT DISPATCHED.** Authored ahead of the real dispatch flow because `ctam-reference-data` does not yet exist as a repo and this story's epic dependencies are not yet `done`. `sprint-status.yaml` still shows this story as `backlog`. Before this is real: confirm the repo exists, run `scripts/dispatch-preflight.sh <story-id>`, re-verify `bus_version` against the repo's actual `_arch/` pin, then land it per delivery-operating-model.md's DISPATCH BRANCH step and flip `sprint-status.yaml` to `ready-for-dev` in the same change.

# Story 1.5.2: Reference Data read-only REST API with jurisdiction filtering, versioning, OpenAPI, RFC 9457 errors

Status: ready-for-dev

## Story

As an **API consumer** (the CTAM user interface today; other CTAM services from Phase 1 onward; and, eventually, the external case-management systems that will start drawing on CTAM data once it manages more than judge availability and scheduling),
I want a versioned, **read-only** reference data API over both the CTAM-owned data and the judge/tribunal-member data from the JOH data source, with responses filtered to the caller's own jurisdiction, a full published API specification, standard "problem details" error responses, and automatic warnings when an endpoint is being phased out,
so that **every service built from this point on can look up controlled lists and judge/tribunal-member reference data at runtime, scoped to the jurisdiction the caller is working in, and the programme's approach to building read-only APIs is proven out on this low-risk service before it's relied on anywhere else**.

## Acceptance Criteria

1. **Given** `ctam-reference-data` carries both the CTAM-owned tables and their seed data, and the judge/tribunal-member data brought in by the earlier data-sync work, **when** the engineer implements the read endpoints, **then** `GET /v1/reference-data/regions`, `/offices`, `/calendar`, and `/vocabularies/{list}` (serving the CTAM-owned data) and `GET /v1/reference-data/johs`, `/jurisdictions`, and `/tickets` (serving the JOH-sourced data) all return a `200 OK` response with structured JSON; **and** every read endpoint requires the caller to be authenticated, though any authenticated caller may read from them; **and** every response is automatically filtered to the caller's jurisdiction — for example, a caller scoped to Employment Tribunals only sees Tribunals/ET-relevant entries — using the jurisdiction hierarchy to correctly include or exclude parent and child jurisdictions; **and** the API never mixes the two kinds of data in a single response — each resource clearly documents which one it serves; **and** there are no endpoints for creating, changing, or deleting data (`POST`, `PUT`, `PATCH`, `DELETE`) — attempting one is rejected with a `405 Method Not Allowed` response, in the standard "problem details" error format, explaining the correct way to make that kind of change (corrections at source for judge/tribunal-member data; the DBA runbook for CTAM-owned data); **and** a full, machine-readable API specification is automatically generated, documenting every read endpoint's requests and responses.

2. **Given** the engineer implements pagination and filtering, **when** a consumer requests, for example, `GET /v1/reference-data/offices?region=northern&page=2&size=50`, **then** the response comes back in a standard paged format — an `items` list, plus `page`, `size`, `totalElements`, and `totalPages` — so consumers can page through large lists consistently; **and** an invalid query parameter returns a `400 Bad Request` response in the standard "problem details" format.

3. **Given** the API specification is generated and automatically checked for quality, **when** the specification is built, **then** it passes that automated quality check; **and** it is published as a versioned package (`uk.gov.hmcts.ctam:api-ctam-reference-data:1.0.0`) to the programme's shared internal package repository, so other teams can pull in a matching client; **and** a browsable version of the specification (Swagger UI) is made available for developers, restricted to internal use at the API gateway.

4. **Given** the API gateway is configured for `ctam-reference-data`, **when** a response leaves the gateway on its way to the caller, **then** it carries standard rate-limit information in its headers; **and** any endpoint flagged in the specification as being phased out automatically gets `Deprecation` and `Sunset` warning headers added (none are flagged yet at this stage of the programme; the mechanism itself is proven with a dedicated test endpoint); **and** internal diagnostic (`/actuator/*`) addresses are blocked from outside access at the gateway.

5. **Given** the engineer publishes the programme's first Postman collection, **when** that collection is run automatically as part of CI, **then** `postman/ctam-reference-data-phase0.postman_collection.json` exercises every read endpoint across both the CTAM-owned and JOH-sourced data; **and** it checks: the normal successful case; that two callers in different jurisdictions see different results; an invalid query correctly returning a 400; an unauthenticated request correctly being rejected; and an attempted write correctly being rejected; **and** the collection is version-controlled alongside the service.

## Tasks / Subtasks

- [ ] Task 1 — CTAM-owned read endpoints, success path (AC: 1)
  - [ ] Write the failing `@WebMvcTest` slice test for `GET /v1/reference-data/regions` returning `200 OK` with the expected JSON shape (C1 — contract test before controller); run it; capture the assertion failure
  - [ ] Write the controller method, minimum to pass; re-run; capture the pass
  - [ ] Repeat the same red/green cycle for `/offices`, `/calendar`, and `/vocabularies/{list}`
- [ ] Task 2 — JOH-sourced read endpoints, success path, and the no-mixing rule (AC: 1)
  - [ ] Write the failing `@WebMvcTest` slice test for `GET /v1/reference-data/johs` returning `200 OK`; run; capture red
  - [ ] Write the controller method reading tier-(a) `jo_*` tables read-only; re-run; capture green
  - [ ] Repeat for `/jurisdictions` and `/tickets`
  - [ ] Write the failing test asserting each resource's OpenAPI schema documents exactly one data source (CTAM-owned or JOH-sourced) and never both in one response; make it pass
- [ ] Task 3 — Authentication required on every endpoint (AC: 1)
  - [ ] Write the failing test asserting an unauthenticated request to each of the 7 endpoints returns `401 Unauthorized` with an RFC 9457 body
  - [ ] Wire the shared `JWTFilter` path; re-run to green
- [ ] Task 4 — Jurisdiction filtering (AC: 1)
  - [ ] Write the failing test asserting a caller scoped to Tribunals/ET only sees ET-relevant rows across the resources that carry a jurisdiction, using two seeded callers with different `AuthDetails` jurisdiction scope
  - [ ] Write the failing test asserting parent/child jurisdiction inclusion/exclusion against the `jo_jurisdictions` hierarchy (e.g. a caller scoped to a parent jurisdiction sees child-jurisdiction rows; a caller scoped to a sibling does not)
  - [ ] Implement the jurisdiction filter (reading scope from `AuthDetails`, never from the raw JWT — S10) on the relevant repositories/queries; re-run both tests to green
- [ ] Task 5 — Write methods rejected (AC: 1)
  - [ ] Write the failing test asserting `POST`, `PUT`, `PATCH`, `DELETE` on each resource return `405 Method Not Allowed` with an RFC 9457 body whose `detail` names the correct change path (DBA runbook for CTAM-owned; source-system correction for JOH-sourced)
  - [ ] Confirm no controller method exists for any of those verbs; re-run to green
- [ ] Task 6 — Pagination and filtering (AC: 2)
  - [ ] Write the failing test asserting `GET /v1/reference-data/offices?region=northern&page=2&size=50` returns `{ items, page, size, totalElements, totalPages }`
  - [ ] Implement offset-based pagination per the shared convention; re-run to green
  - [ ] Write the failing test asserting an invalid query parameter (e.g. non-numeric `page`) returns `400 Bad Request` in RFC 9457 form
  - [ ] Implement the parse-failure path (C9 — 400 is for what the parser cannot read, not a 422); re-run to green
- [ ] Task 7 — OpenAPI generation and Spectral (AC: 1, 3)
  - [ ] Confirm springdoc/Swagger Core generates the spec covering all 7 endpoints, their query parameters, and their response/error shapes (C6 — no undeclared surface)
  - [ ] Run the Spectral lint via `./scripts/verify.sh`; fix any convention breach until clean (C3)
- [ ] Task 8 — Publish the spec artefact and Swagger UI (AC: 3)
  - [ ] Wire Gradle `maven-publish` to publish `uk.gov.hmcts.ctam:api-ctam-reference-data:1.0.0` to the internal artefact repository (C5 — producers publish)
  - [ ] Confirm the gateway exposes the browsable Swagger UI internally only, per the epic's AC
- [ ] Task 9 — Gateway posture: rate-limit headers, Deprecation/Sunset mechanism, actuator blocking (AC: 4)
  - [ ] Write the failing contract/integration test asserting a dedicated test endpoint flagged deprecated in the spec carries `Deprecation` and `Sunset` response headers (RFC 9745 / RFC 8594 formats)
  - [ ] Wire the gateway policy/annotation that adds those headers for flagged endpoints; re-run to green
  - [ ] Confirm (via the gateway configuration, not application code) that `/actuator/*` is blocked externally and that rate-limit headers are present on gateway responses
- [ ] Task 10 — Postman collection (AC: 5)
  - [ ] Author `postman/ctam-reference-data-phase0.postman_collection.json` covering: success path per endpoint; two-jurisdiction differential result check; invalid-query 400; unauthenticated 401; attempted-write 405
  - [ ] Wire the collection into CI; run it; capture the pass

## Dev Notes

### Context distilled from the bus

- Versioning and deprecation: `_arch/architecture.md` — "API versioning: URI prefix major versioning… `/v1/johs`, `/v2/johs`. Backwards-compatible additions stay within the major version. Deprecation signalling: `Deprecation` header per RFC 9745 (date-stamp value, e.g. `Deprecation: @1735689600`) + `Sunset` header per RFC 8594 (RFC-date value); minimum 6-month internal / 12-month external window before removal." Matches `_arch/agent-rules/30-api-contracts.md` C4 (`/v1` is append-only within a major version).
- Publishing: `_arch/architecture.md` — "Each service's OpenAPI 3.x spec is published by Gradle (via the `maven-publish` plugin) as a Maven-format artefact (`uk.gov.hmcts.ctam:api-ctam-{service}:{version}`) to the internal artefact repository; consumers pull it at compile time. Postman collections per phase derived from the spec." This is the source for the exact artefact coordinate in AC-3.
- Jurisdiction hierarchy: `_arch/architecture/data-tables.md` — `jo_jurisdictions` is "the first-class rollout/authorisation/filtering dimension… parent-child shape preserved natively if upstream provides it, or established on ingest" (D8: "rollout is jurisdiction-first, then per-region; jurisdiction is a first-class hierarchical attribute").
- Jurisdiction scope comes from the authorisation context, never the raw token: `_arch/agent-rules/50-security-and-logging.md` S10 — "Roles, jurisdiction, Region/Area scope and the activation flag come from the Authorisation service's response, held in `AuthDetails` — not from JWT claims directly." `_arch/architecture.md`'s cross-cutting table cites `config/RegionScopeFilter.java` as the per-service file that scopes by jurisdiction and Region/Area from the authz context.
- Rate limiting and gateway posture: `_arch/architecture.md`'s integration-points diagram marks Azure API Management as where "rate limits applied here"; `/actuator/*` is "ops-restricted at the APIM layer" (`_arch/architecture.md`, Observability section).
- Error shape and status-code table: `_arch/architecture/conventions.md` → *Format Patterns* (HTTP status table) and *Process Patterns* (`@ControllerAdvice` mapping). 405 for a disallowed verb is not in the fixed exception→status table in `_arch/agent-rules/30-api-contracts.md` C7 — it is a routing-level rejection (no matching handler), consistent with C6 ("nothing in the spec is unimplemented") rather than a mapped exception; still returned as RFC 9457 per C7's blanket rule that "errors are `application/problem+json`, always."
- Pagination shape: `_arch/architecture/conventions.md` → *Format Patterns* — "Offset-based for small filtered lists: `?page=0&size=20`. Response: `{ items, totalElements, page, size }`." The epic's AC additionally asks for `totalPages`, which is an additive, non-breaking field on top of the documented shape.

### Rules that apply

- [`_arch/agent-rules/30-api-contracts.md`](../../../../../../ctam-architecture/agent-rules/30-api-contracts.md) — C1 (contract test before controller, for every one of the 7 endpoints), C3 (Spectral must pass), C4 (`/v1` append-only), C5 (producers publish the spec artefact), C6 (no undeclared surface — every query param and status code in the spec), C7 (RFC 9457 via `@ControllerAdvice`), C9 (400 vs 422 — the invalid-query-parameter case here is a 400, not a 422, because it is a parse failure), C11 (response DTOs are immutable records).
- [`_arch/agent-rules/50-security-and-logging.md`](../../../../../../ctam-architecture/agent-rules/50-security-and-logging.md) — S9 (the `JWTFilter` path is the only path), S10 (jurisdiction/scope from `AuthDetails`, never the JWT claims directly), S11 (authorise every endpoint explicitly — here, "any authenticated caller" is the stated requirement per AC-1), S1/S2 (no personnel numbers or PII in logs when logging jurisdiction-filtered query activity).
- [`_arch/agent-rules/40-data-and-liquibase.md`](../../../../../../ctam-architecture/agent-rules/40-data-and-liquibase.md) — P5 (`jo_*` tables are read-only; no writable JPA entity maps to them), P10 (no N+1 — list endpoints always paged; no unbounded `findAll()` reaching a controller), P11 (repositories use derived queries or `@Query` with named parameters, never string-concatenated SQL).
- [`_arch/agent-rules/10-tdd.md`](../../../../../../ctam-architecture/agent-rules/10-tdd.md) — T7 (Spring context tests rationed — controller behaviour uses `@WebMvcTest`, not a full `@SpringBootTest`), T9 (every endpoint gets a contract/Pact test, both sides where consumed).
- [`_arch/agent-rules/00-core.md`](../../../../../../ctam-architecture/agent-rules/00-core.md) — R10 (contract before controller), R11 (never log what must not leak).

### Project Structure Notes

- Controllers in `src/main/java/uk/gov/hmcts/ctam/referencedata/controller/`, one per resource family (CTAM-owned vs JOH-sourced, per the no-mixing rule); DTOs in `dto/`; the jurisdiction-scoping filter in `config/RegionScopeFilter.java`; error handling in `error/GlobalExceptionHandler.java` and `error/ProblemDetailFactory.java` — all per `_arch/architecture/repo-structure.md`'s per-service tree and `_arch/architecture.md`'s cross-cutting-concerns table.
- OpenAPI wiring in `config/OpenApiConfig.java` (Swagger Core), per the same cross-cutting-concerns table.
- Postman collection at `postman/ctam-reference-data-phase0.postman_collection.json`, per `_arch/architecture/repo-structure.md` and the epic's own "first Postman collection" framing.
- The dedicated test endpoint used to prove the `Deprecation`/`Sunset` mechanism (Task 9) is new surface sanctioned directly by AC-4's own wording ("the mechanism itself is proven with a dedicated test endpoint") — cite this AC, not a convention, since none of `conventions.md`/`30-api-contracts.md` names a canonical test-endpoint path; if a specific path isn't obvious from the spec tooling in use, that is an open question for whoever picks it up.

### Out of scope / boundaries

- The reference data endpoint sourced from the separate MRD feed (JOH Specialisations) — a later piece of work (Epic 1.6) building on top of this API rather than standing up its own.
- Admin write endpoints for the CTAM-owned data — none exist through this API in any phase covered here; changes go through the DBA runbook from Story 1.5.1.
- Any write surface at all for the judge/tribunal-member data — that never happens, in any phase; corrections happen at the JOH eLinks source and arrive on the next sync (Story 1.2.1).
- Actually provisioning or configuring the Azure API Management gateway resource itself — that is shared infrastructure; this story only proves the application-level and gateway-policy behaviour that depends on it (rate-limit headers present, actuator blocked, Deprecation/Sunset headers on flagged endpoints).

### Definition of done

Per [`_arch/agent-rules/90-definition-of-done.md`](../../../../../../ctam-architecture/agent-rules/90-definition-of-done.md), applied with these deltas for this story:

- Q6 (contract test + Spectral) applies to all 7 read endpoints; the 405-on-write behaviour is covered by the same contract-test discipline even though there is no corresponding controller method.
- Q7 (schema-change `*IT`) is **not applicable** — this story adds no Liquibase changeset of its own; it reads tables created in Story 1.5.1 and the tier-(a) tables from Epic 1.1.

### References

- [Source: _arch/architecture.md — API versioning / Deprecation & Sunset headers; OpenAPI publishing to the internal artefact repository; APIM rate limits and `/actuator/*` restriction]
- [Source: _arch/architecture/data-tables.md — `jo_jurisdictions` parent-child hierarchy as the filtering dimension; D8 jurisdiction-first rollout]
- [Source: _arch/architecture/conventions.md → Naming Patterns (API endpoints), Format Patterns (pagination, status codes), Process Patterns (`@ControllerAdvice` mapping)]
- [Source: _arch/agent-rules/30-api-contracts.md — C1, C3, C4, C5, C6, C7, C9, C11]
- [Source: _arch/agent-rules/50-security-and-logging.md — S9, S10, S11]
- [Source: _arch/agent-rules/40-data-and-liquibase.md — P5, P10, P11]
- [Source: _arch/agent-rules/10-tdd.md — T7, T9]
- [Source: _bmad-output/planning-artifacts/epics/fr-coverage-map.md — FR7 row, FR58 row]

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List
