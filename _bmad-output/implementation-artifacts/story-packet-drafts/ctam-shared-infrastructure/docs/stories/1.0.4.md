---
story_id: 1.0.4
epic: epic-1.0-platform-estate-provisioned
repo: ctam-shared-infrastructure
bus_version: arch-v1.1
frs: []
nfrs: [NFR25, NFR26, NFR27]
depends_on_stories: ["1.0.2"]
sprint_status_key: 1-0-4-the-container-registry-and-monitoring-setup-are-built-and-checked-development-environment
---

> **DRAFT — NOT DISPATCHED.** Authored ahead of the real dispatch flow because `ctam-shared-infrastructure` does not yet exist as a repo. `sprint-status.yaml` still shows this story as `backlog`. Before this is real: create the repo, run `scripts/dispatch-preflight.sh <story-id>`, re-verify `bus_version` against the repo's actual `_arch/` pin, then land it per delivery-operating-model.md's DISPATCH BRANCH step and flip `sprint-status.yaml` to `ready-for-dev` in the same change.

# Story 1.0.4: The container registry and monitoring setup are built and checked (development environment)

Status: ready-for-dev

## Story

As a **platform engineer**,
I want a container image registry and an application-monitoring setup built through Terraform and connected to the cluster,
so that **there's a proven way to distribute container images and a proven place for logs and traces to land, before any service ships a container or writes a log line**.

## Acceptance Criteria

1. Given the cluster exists as described in Story 1.0.2, when the engineer adds the registry and monitoring setup and applies it to the development environment, a container image registry is provisioned, with a resilient setup, and the cluster is granted permission to pull images from it using its own managed identity rather than a stored registry password.
2. An application-monitoring workspace (backed by a log-analytics store) is provisioned in the UK South region.
3. How long logs are kept is set to the agreed early-phase default of 90 days for non-production environments (subject to final confirmation), configured through Terraform.
4. The connection details for the monitoring workspace are stored in the vault, not committed to the repository.
5. Given the registry and monitoring setup have been built, when the engineer runs the verification, a test container image can be pushed to the registry and then successfully pulled back down into the cluster, confirming the image supply chain works end to end.
6. A test trace and a structured log entry sent from a workload running inside the cluster both show up in the monitoring workspace within the expected time window, confirming the logging and tracing pipeline genuinely works.
7. These checks are written down as repeatable verification steps.

## Tasks / Subtasks

- [ ] Task 1 (AC: 1): Write `terraform/modules/acr/` (registry, resilient SKU) and grant AKS's managed identity `AcrPull` — no stored registry credential anywhere.
- [ ] Task 2 (AC: 2, 3): Write `terraform/modules/observability/` (App Insights + Log Analytics workspace, UK South, 90-day retention for non-prod set via Terraform variable — flag the "subject to final confirmation" caveat from the epic as an *Open Question* if the 90-day figure isn't independently confirmed elsewhere by dispatch time, per R5).
- [ ] Task 3 (AC: 4): Store the workspace's connection string / instrumentation key in the Key Vault provisioned in Story 1.0.3; do not commit it.
- [ ] Task 4 (AC: 5, 7): Write `verification/acr-observability.sh`'s image-supply-chain half — push a test image, pull it back into the cluster. Capture red (before the module exists — push target doesn't resolve) then green.
- [ ] Task 5 (AC: 6, 7): Extend `verification/acr-observability.sh` with the log/trace half — emit a test trace and a structured log line from an in-cluster workload, poll the monitoring workspace until both appear within the expected window, capture red/green.

## Dev Notes

### Context distilled from the bus

- **AC-verified dependency:** the AC text says "the cluster exists as described in [the earlier story]" — the cluster from Story 1.0.2, not the database/vault from 1.0.3. `depends_on_stories` is `["1.0.2"]`, matching Story 1.0.3's note: 1.0.3, 1.0.4, and 1.0.5 are independently gated on 1.0.2 alone, not chained to each other, even though the epic narrates them in a fixed 1.0.3 → 1.0.4 → 1.0.5 order. This story does, however, need the vault from 1.0.3 for AC 4 (storing the workspace connection details) — that is a genuine build-order dependency the AC's own "Given" clause doesn't name explicitly. Flag this at dispatch time: in practice, land 1.0.3 before 1.0.4 even though the formal `depends_on_stories` graph (built from the "Given" clauses alone) does not require it.
- [Source: `_arch/architecture/repo-structure.md` → `ctam-shared-infrastructure` tree]: `terraform/modules/acr/` = "container registry"; `terraform/modules/observability/` = "App Insights + Log Analytics + retention"; the verification script for this whole layer is named `verification/acr-observability.sh` ("image push/pull; test trace + log land in App Insights") — one script covering both halves of this story's ACs.
- **On FR/NFR coverage:** this is the one story in the epic that maps cleanly onto part of the declared coverage set. [Source: `_bmad-output/planning-artifacts/implementation-readiness-report-2026-05-15.md`]: NFR25 ("structured logs with consistent fields, correlation IDs... logging schema is Phase 0 deliverable"), NFR26 ("log retention sufficient for pilot incident triage; specific period in Phase 0"), NFR27 ("logs ingested into Azure-native logging — App Insights / Log Analytics") are exactly what AC 2–3 and AC 6 build and prove. NFR28 (per-service Kubernetes liveness/readiness probes via Spring Actuator) is **not** claimed here — that is delivered by each service's own code, not by provisioning the monitoring workspace, so it is left off this story's `nfrs`. NFR42 (Postman collections per phase) also does not apply to an infra-only repo with no API surface of its own.

### Rules that apply

Per `agent-rules/index.md` ("Who this binds"), `ctam-shared-infrastructure` has **no Terraform-specific rule pack** — only `_arch/agent-rules/00-core.md`'s core rules apply, by conduct, with no enforcer:

- **R1** — scope is this story's ACs; per-service dashboards/alerts are explicitly deferred (see Out of scope).
- **R2** — red-then-green evidence for both halves of `verification/acr-observability.sh`.
- **R5** — the 90-day retention figure is explicitly flagged by the epic itself as "subject to final confirmation" — do not silently firm it up; if it's still unconfirmed at dispatch time, record it under *Open Questions* rather than treating 90 as settled.
- **R6** — no per-service log-analytics query packs, workbooks, or extra ACR replication beyond what this story's ACs call for.
- **R11** — the instrumentation key / connection string is a secret; it goes in the vault (AC 4) and never appears in a pasted evidence log.
- **R12** — the push/pull and trace/log evidence must be from an actual run, not a description of expected behaviour.

### Project Structure Notes

Adds to the Stories 1.0.1–1.0.3 scaffold, per [Source: `_arch/architecture/repo-structure.md`]:

```
ctam-shared-infrastructure/
├── terraform/
│   └── modules/
│       ├── acr/            (new this story)
│       └── observability/  (new this story)
└── verification/
    └── acr-observability.sh (new this story)
```

### Out of scope / boundaries

- Dashboards or alerts specific to any individual service — those get added as each service is built.
- The API gateway — Story 1.0.5.

### Definition of done

As in the earlier stories: `_arch/agent-rules/90-definition-of-done.md` was not read while drafting; no Terraform-equivalent Q-gate exists yet. "Done" means the registry push/pull and the trace/log ingestion are both proven with pasted evidence, the 90-day retention figure's confirmation status is explicitly recorded (settled or still open), and no secret value is pasted anywhere (R11).

### References

- [Source: `_bmad-output/planning-artifacts/epics/phase-1/epic-1.0-platform-estate-provisioned.md` → Story 1.0.4]
- [Source: `_arch/architecture/repo-structure.md` → `ctam-shared-infrastructure` tree, `verification/` scripts]
- [Source: `_bmad-output/planning-artifacts/epics/framework.md` → Phase 1 × Platform & DevEx row]
- [Source: `_bmad-output/planning-artifacts/implementation-readiness-report-2026-05-15.md` → NFR25–NFR28 definitions]
- [Source: `_arch/agent-rules/00-core.md` → R1–R14]
- [Source: `_arch/agent-rules/index.md` → "Who this binds"]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
