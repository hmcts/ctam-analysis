---
type: 'Architecture Shard'
description: 'Architecture Changelog'
resource: 'architecture/tobe/changelog.html'
tags: [ctam-pathfinder, architecture]
timestamp: '2026-06-11'
parent: ../architecture.md
title: Architecture Changelog
last_updated: 2026-06-11
extracted_in: architecture.md v1.8 — Strategy B refactor
---

# Architecture Changelog

> Sibling of [`../architecture.md`](../architecture.md). The parent file links here at its tail.
>
> **Anchor convention:** entries authored at v1.0–v1.7 reference section anchors that lived in the original single-file `architecture.md`. v1.8 split those sections into siblings under `architecture/`. Pre-v1.8 anchors are preserved verbatim (audit trail) and resolve in the sibling files via the table below.

## Pre-v1.8 anchor → current location

| Old anchor (`architecture.md#…`) | Now lives in |
|---|---|
| `#starter-template-evaluation` (Step 3 starter content) | [`./starter-template.md`](./starter-template.md) |
| `#per-service-ctam-conventions-encoded-in-scaffolding-script-not-in-shared-library` | [`./starter-template.md` → "Per-service CTAM Pathfinder Conventions"](./starter-template.md) |
| `#data-architecture` (Authoritative Table Ownership Mapping subsection) | [`./data-tables.md`](./data-tables.md) |
| `#implementation-patterns--consistency-rules` (Step 5 entirely) | [`./conventions.md`](./conventions.md) |
| `#naming-patterns` | [`./conventions.md` → "Naming Patterns"](./conventions.md) |
| `#test-conventions` | [`./conventions.md` → "Process Patterns"](./conventions.md) |
| `#complete-project-directory-structure--per-backend-service` | [`./repo-structure.md`](./repo-structure.md) |
| `#documented-gaps` (G1–G6) | [`./gaps.md`](./gaps.md) |
| `#assumptions` (A1–A33) | [`./assumptions.md`](./assumptions.md) |
| Other anchors (foundational principles, core architectural decisions, validation results, integration points, etc.) | unchanged — still in [`../architecture.md`](../architecture.md) |

## Versions

| Version | Date | Summary | Sections affected |
|---|---|---|---|
| **v4.9 — Phase 0 repo-impact review: `ctam-architecture` enablement promoted to Epics 0.A/0.B; per-service DB role creation assigned; cross-repo story attribution recorded** | 2026-08-18 | **SCP 2026-08-18e** — a review of Phase 0 from the perspective of *which repos each epic and story actually touches*. **No FR, NFR, decision or scope change; no code impact** (nothing dispatched, every story `not-started`). Ten findings, all remediated pre-dispatch. **The repo coverage itself was correct** — Phase 0 + Phase 0-Mock touch **9 of the 18 product repos** plus `ctam-analysis`, and no epic reaches into a later phase's repo. What was wrong was *under*-declaration. **(1) Two new epics, lettered so no existing story id moved** (the `0M.` convention): **Epic 0.A** (`ctam-architecture`, 3 stories, `depends_on: []` — **precedes Epic 0.0**) delivers the `arch-v1.0` bus tag, **`ctam-scaffold.sh`** + the starter overlay, and **`runbooks/github-setup.md`** + **`runbooks/terraform.md`** — work that **eight stories already opened by assuming** (0.0.1, 0.2.1, 0.4.1, 0.4.2, 0.8.1, 0M.1.1, 0M.2.1) and that sat in an undecomposed `arch-baseline` node with no stories, no ledger shard and no owner; **Epic 0.B** (1 story, `depends_on: [epic-0.A, epic-0.0]`) delivers the `ctam_configuration_values` baseline (FR8) **plus every per-service DB role and the grants convention**. **Split in two deliberately:** 0.A must precede Epic 0.0 (Story 0.0.1 cites its runbook) while the baseline needs the Story 0.0.3 server — one epic would have made the two nodes mutually dependent, a **latent cycle that already existed** in `arch-baseline`'s `depends_on: [epic-0.0]`. **(2) Per-service DB role creation had no owner** — Story 0.0.3 excluded it, `arch-baseline` never mentioned it, and Stories 0.2.2 / 0.5.1 granted SELECT to "placeholder roles for future services" that nothing created; **Story 0.2.2's first `liquibase update` would have failed**. Now Story 0.B.1. **(3) Per-service APIM registration ownership resolved** — the instance, base policies and WAF stay in `ctam-shared-infrastructure`; each **service's own API definition + per-API policy** live in that service's `terraform/`, applied via the cross-repo pattern recorded in `runbooks/terraform.md` (G10.2). Recorded as an AR53 clarification in [`./conventions.md`](./conventions.md). **(4) New ledger fields `touches:` and `note:`** — `repo:` stays single-valued (the claim lock), `touches:` names secondary repos a story **commits an artefact** into (six stories: 0.1.1 → the bus; 0.2.2/0.3.1/0.4.3/0.8.1 → `data-tables.md`; 0.5.1 → both, incl. `runbooks/reference-data-maintenance.md`), `note:` a consumption-only constraint. **(5) New `repo_serialisation` block** in the dispatch graph — Epics 0.5 and 0.6 are graph-independent but land in one repo and rewrite the **same** OpenAPI artefact and Postman collection; order is `0.2 → 0.3 → 0.5 → 0.6` (`ctam-reference-data`) and `0.A → 0.B → 0.1.2 → 0.7` (`ctam-architecture`). **(6) Port collision fixed and a per-service allocation table added** (AR3 amended) — `8082` was assigned to `ctam-reference-data`, `ctam-authorisation` **and** `ctam-notification`, colliding in exactly the multi-service local stack the Phase 0 demo gate requires (raised in `implementation-readiness-report-2026-08-11.md` §2 and still open). Now services **8082–8092**, non-production mocks **8100–8102** (`ctam-mrd-mock` 8091 → 8102), SPAs on Vite 5173/5174. **(7) Fixture alignment** — Stories 0.4.2, 0.5.1 and 0.7.1 now key off the **ET predicate and shared identity set published by Story 0M.1.2** instead of three independently-derived definitions. **(8) Perimeter exceptions named** — Story 0.0.6 asserted the estate is unreachable except through APIM while `ctam-ui` provisions a public SWA (0.4.4, hosting still open per G3.5) and `ctam-reference-data` provisions a container an **external team must write into** (0.3.1); each is now a documented exception with compensating controls, an owning repo, and a security sign-off gate. **(9) Renumbering drift cleared** — Epic 0.0's "Epic 0.1 Story 0.1.1" → 0.2.1 and "Story 0.1.4" → 0.3.1; Epic 0.4's two "Epic 0.6" → 0.7; [`./delivery-operating-model.md`](./delivery-operating-model.md)'s ledger example and **bootstrapping order** rewritten (it scaffolded Terraform-only `ctam-shared-infrastructure` with the Java scaffolder and named a parallel set the current graph forbids — `ctam-mock-auth` is Story 0.4.2 *inside* Epic 0.4). **(10) `ctam-analysis` declared in the repo inventory** as the 19th repo / control plane — it appears as a `repo:` value in the dispatch graph but was absent from the 18-repo table; **18 remain product repos**. **Phase 0: 9 → 11 epics, 23 → 27 stories** (Phase 0-Mock unchanged at 2/4); graph re-verified acyclic with exactly one zero-dependency node. **Deferred, recorded not actioned:** the `authz/check` vs `/v1/authz/check` inconsistency (AR38 mandates `/v1/`; needs one deliberate sweep of `architecture.md`, `conventions.md`, `project-context.md` and Epic 0.4) and Epic 0.2's "NFR59" typo for FR59. | **new** [`../epics/phase-0/epic-0.A-architecture-context-bus-and-scaffolding.md`](../epics/phase-0/epic-0.A-architecture-context-bus-and-scaffolding.md) + [`epic-0.B-shared-db-baseline-and-service-roles.md`](../epics/phase-0/epic-0.B-shared-db-baseline-and-service-roles.md); all eight existing Phase 0 epics edited ([0.0](../epics/phase-0/epic-0.0-platform-estate-provisioned.md) stale refs + Terraform-only note + perimeter-exception AC, [0.1](../epics/phase-0/epic-0.1-postgres-schema-design.md), [0.2](../epics/phase-0/epic-0.2-joh-reference-data-ingested.md) port/APIM/roles/CI-gates, [0.3](../epics/phase-0/epic-0.3-mrd-reference-data-ingested.md) drop-container exception, [0.4](../epics/phase-0/epic-0.4-user-authenticates.md) ports/roster/SWA/0.6→0.7, [0.5](../epics/phase-0/epic-0.5-joh-reference-data-read-only-api.md) cross-repo shape + ET predicate, [0.6](../epics/phase-0/epic-0.6-mrd-reference-data-read-only-api.md) serialisation, [0.7](../epics/phase-0/epic-0.7-user-populations-bootstrapped.md) cross-repo write shape, [0.8](../epics/phase-0/epic-0.8-system-dispatches-emails.md) port/role/APIM); [`../epics/phase-0-mock/epic-0M.2-mrd-reference-data-mocked.md`](../epics/phase-0-mock/epic-0M.2-mrd-reference-data-mocked.md) (port); [`../epics/phase-0/index.md`](../epics/phase-0/index.md) (**new Repo column + "Repo coverage at a glance" section**, sequencing, scope model, 2 summaries, totals); [`../epics/index.md`](../epics/index.md); [`../epics/framework.md`](../epics/framework.md); [`../epics/requirements-inventory.md`](../epics/requirements-inventory.md) (**AR3 amended**, AR53 clarified); `delivery/dispatch-graph.yaml` (`arch-baseline` → `epic-0.A`+`epic-0.B`, 6 `depends_on` rewired, `epic-0.1` `repo:` list, **new `repo_serialisation`**); **new** `delivery/ledger/epic-0.A.yaml` + `epic-0.B.yaml` (11 → 13 shards), `touches:`/`note:` on 7 shards; [`delivery/ledger/README.md`](../delivery/ledger/README.md) (**schema + new `repo`/`touches`/`note` section**), [`delivery/README.md`](../delivery/README.md); [`./conventions.md`](./conventions.md) (**port allocation table + APIM ownership rule**); [`./repository-strategy.md`](./repository-strategy.md) (`ctam-analysis`); [`./delivery-operating-model.md`](./delivery-operating-model.md) (ledger example, **bootstrapping order**, parallel-execution caveat); [`./gaps.md`](./gaps.md) (G1.4a/G1.4b/G4.4/G4.5/G9.1/G10.1/G10.2 owners, G6.4); `scripts/python/build_html.py` NAV; [SCP](../sprint-change-proposal-2026-08-18e.md) |
| **v4.8 — eLinks v5 mock adopted brownfield; Node/Express accepted as the one AR2–AR17 exception; four architecture assumptions falsified** | 2026-08-18 | **Decision #19** (SCP 2026-08-18d). **Epic 0M.1 rewritten from a greenfield build into a brownfield adoption.** A working mock of the **Judicial Office eLinks People API v5** already existed — written from vendor documentation (`Swagger UI.pdf`, `apiresponses.docx`) and serving the **real production reference-data exports** of 2026-06-01 (Locations 1,999 · BaseLocations 1,461 · AppointmentTitles 193 · JudiciaryRoles 163 · Tickets 158; organisational data only, no personal data) with synthetic deterministic people. Epic 0M.1's original premise — scaffold a Spring Boot service and serve fixtures shaped by *guessing* the contract from `data-tables.md` — is obsolete. The epic now covers **adoption + hardening + the two capabilities the implementation lacks**: **Story 0M.1.1** (rename to `ctam-joh-mock`, container, Helm chart, k8s probes on endpoints outside the mocked `/api/v5` surface, structured JSON logs + correlation IDs into shared App Insights, the **production-refusal guards — currently absent**, CI with reproducible install + tests + lint + `npm audit` + CycloneDX SBOM + `helm lint`, retention of the contract-provenance documents, and an **NFR14 forbidden-data check plus data-owner sign-off** on the production exports) and **Story 0M.1.2** (machine-readable **OpenAPI 3.x** with each field marked *evidenced*/*inferred*, a **guaranteed Employment Tribunals cohort**, fixture-identity alignment across all three mocks and the Epic 0.7 bootstrap, and **fault injection — also currently absent**, including change-feed-specific faults no existing AC covers). **Stack: Node 22 LTS + Express 4, accepted as the single documented exception to AR2–AR17 — new AR55.1**, scoped to **language and build tooling only**; operability, observability, security, SBOM and contract discipline are all still required, and the exception is explicitly **not precedent**. `AR55` revised accordingly; [`./conventions.md`](./conventions.md)'s scaffold rule carries a matching carve-out. **`epic-0M.1` drops `epic-0.1` from `depends_on`** — the mock's shape now derives from the real documented contract rather than CTAM's `jo_*` schema, inverting the information flow (Epic 0.1's schema should be validated *against* this contract). `epic-0.2` still depends on both, so nothing downstream is weakened; graph re-verified acyclic. **FOUR LOAD-BEARING ASSUMPTIONS ARE FALSIFIED BY THIS EVIDENCE, RECORDED AND DELIBERATELY NOT YET ACTIONED** (findings F1–F4, in full on Epic 0M.1 and in [`./gaps.md`](./gaps.md) G8.1): **F1** the API is a **change feed** (`people?updated_since=`) with **explicit** `leavers`/`deleted` feeds, not a full-refresh dump — **contradicts AR46** and its "rows absent upstream are marked inactive" rule; **F2** `REF_Jurisdiction` is **flat** (5 rows, no `parent_id`) — the hierarchy lives on `locations.parent_id` / `base_locations.parent_id` / `ticket_categories.parent_category_id`, **contradicting D8 and G8.1's "derivable on ingest" assumption**; **F3** **Employment Tribunals is a *location* (1036, `parent_id` 1675, `jurisdiction_id` 34 Tribunals) plus a ticket set and 10 appointment titles — not a jurisdiction**, so G8.1's wave-1 condition cannot be satisfied as written and **FR57's `(jurisdiction, region)` activation key cannot express ET**; **F4** the contract exposes **`per_id`** and **`personal_code`** and has **no `personnel_number`** — the field **D9**'s identity chain and AR46 both name as the upstream natural key. The AR46 / D8 / D9 / FR57 corrections are a separate architect-led change (SCP 2026-08-18d, decision 4); **until it lands the artifacts and the evidence disagree**, and any story dispatched against Epics 0.2 / 0.4 / 0.5 / 0.7 must read Epic 0M.1's *Findings* section first. **G8.5 partly evidenced** — ET legal titles (`Employment Judge` 48, `Regional Employment Judge` 71, +7 more, and judiciary role 90004) are **confirmed real**; the employer-side / employee-side **lay-member panels appear nowhere** in the reference data and stay provisional, making panel composition the substantive remainder of the ET as-is pack. **New gap G8.7** — data-owner governance of the production exports held in the repo. **G8.1 stays open**: no live connection, cadence/SLA unconfirmed, MRD half untouched. **No FR or NFR changed. No code impact to any other repo.** | [`../epics/phase-0-mock/epic-0M.1-joh-reference-data-mocked.md`](../epics/phase-0-mock/epic-0M.1-joh-reference-data-mocked.md) (**full rewrite** — adoption framing, what-exists inventory, what-must-be-added list, stack-exception rationale, **new *Findings* section**, both stories replaced); [`../epics/phase-0-mock/index.md`](../epics/phase-0-mock/index.md) (0M.1 summary, epics-table row, demo row, scope-model bullets on deployables / contracts / fixture identity, "does not close G8.1" qualified for the now-asymmetric evidence, validation section); [`../epics/framework.md`](../epics/framework.md) (Phase 0-Mock Area scope + coverage); [`../epics/requirements-inventory.md`](../epics/requirements-inventory.md) (**AR55 revised**, **AR55.1 new**); [`../architecture.md`](../architecture.md) (**decision #19**, *Phasing of Upstream Integrations* `ctam-joh-mock` block rewritten); [`./repository-strategy.md`](./repository-strategy.md) (`ctam-joh-mock` row rewritten, phase `0` → `0-mock` on both mock rows, new mock-as-repo/Node-exception paragraph); [`./conventions.md`](./conventions.md) (scaffold-rule carve-out); [`./gaps.md`](./gaps.md) (**G8.1 major update**, **G8.5 partly evidenced**, **G8.7 new**); `delivery/dispatch-graph.yaml` (`epic-0M.1` `depends_on` + adoption note); `delivery/ledger/epic-0M.1.yaml` (story titles, NFR14 trace, adoption note); `scripts/python/build_html.py` NAV; [SCP](../sprint-change-proposal-2026-08-18d.md) |
| **v4.7 — upstream stand-ins promoted to their own pre-Phase-0 tier, Phase 0-Mock; 0.9/0.10 → 0M.1/0M.2** | 2026-08-18 | **Decision #18** (SCP 2026-08-18c) — **supersedes #18's predecessor #17 on placement and numbering only.** The two upstream stand-in epics introduced yesterday move out of Phase 0 into a **new phase, `epics/phase-0-mock/`**, with its own phase index, its own row in [`../epics/index.md`](../epics/index.md)'s phase table and in [`../epics/framework.md`](../epics/framework.md), and its own site-NAV group. They are **renumbered `0.9` → `0M.1`** (`ctam-joh-mock`) and **`0.10` → `0M.2`** (`ctam-mrd-mock`), story ids `0M.1.1`/`0M.1.2`/`0M.2.1`/`0M.2.2`; ledger shards and dispatch nodes follow (`epic-0M.1`, `epic-0M.2`). **Why:** v4.6 appended the epics at 0.9/0.10 to avoid renumbering 23 stories and paid for it with a standing trap — two epics numbered *after* Epic 0.8 that must build *before* Epic 0.2 — mitigated only by warning notes in five artifacts. An `0M.` prefix in a named tier puts the sequencing information **in the identifier**, so the warnings become unnecessary rather than load-bearing. It also records a structural fact v4.6 obscured: stand-in work is not foundations, it is what makes foundations buildable. **Phase 0-Mock is explicitly not a slot in the 0–9 sequence** — its epics are dependencies *of* Epics 0.2/0.3 and nothing else depends on them. **Counts: Phase 0-Mock 2 epics / 4 stories; Phase 0 back to 9 epics / 23 stories** (v4.6's "11 epics / 27 stories" no longer describes the current state). Build order is documented as a **partial** order — both `0M.` epics unblock together once `epic-0.1` is `done`, don't wait on each other, and parallelise with `epic-0.8`; graph re-verified acyclic. **Unchanged from v4.6:** both repos and their full scope, AR55/AR56, the G5.3 widening, G8.1's de-risked-but-**open** position, the 18-repo count, and `ctam-mock-auth` staying as Story 0.4.2 inside Epic 0.4 — it is an identity-provider mock inside a working auth slice, not an upstream data-source mock, and extracting it would leave Epic 0.4 unable to demo sign-in. **No FR, NFR or code impact** (implementation not started). **Immutable history retained:** [SCP 2026-08-18b](../sprint-change-proposal-2026-08-18b.md) and the v4.6 row below are preserved verbatim and still say 0.9/0.10 and 11 epics / 27 stories — that is the record of that decision, not a description of today. Architecture decision **#17 is marked *placement and numbering superseded by #18*** in place rather than rewritten, following the D11→D13 and AR53-revised convention. | **new** [`../epics/phase-0-mock/index.md`](../epics/phase-0-mock/index.md); `epic-0.9-joh-reference-data-mocked.md` → [`../epics/phase-0-mock/epic-0M.1-joh-reference-data-mocked.md`](../epics/phase-0-mock/epic-0M.1-joh-reference-data-mocked.md); `epic-0.10-mrd-reference-data-mocked.md` → [`epic-0M.2-mrd-reference-data-mocked.md`](../epics/phase-0-mock/epic-0M.2-mrd-reference-data-mocked.md) (both renamed + renumbered internally, front-matter `epic:`/`parent:`/`resource:`/`tags:` retargeted); [`../epics/phase-0/index.md`](../epics/phase-0/index.md) (two epics + summaries + demo rows removed, totals 27 → 23, sequencing note rewritten as a cross-phase pointer, retirement-path paragraph moved out); [`../epics/index.md`](../epics/index.md) (new phase row, phase-0 row reverted, story-id convention note); [`../epics/framework.md`](../epics/framework.md) (new **Phase 0-Mock** heading + the *Upstream Source Stand-Ins* Area moved under it, phase-summary row relabelled); [`../epics/fr-coverage-map.md`](../epics/fr-coverage-map.md); [`../epics/phase-0/epic-0.2-joh-reference-data-ingested.md`](../epics/phase-0/epic-0.2-joh-reference-data-ingested.md) + [`epic-0.3-mrd-reference-data-ingested.md`](../epics/phase-0/epic-0.3-mrd-reference-data-ingested.md) (stand-in refs renumbered); [`../architecture.md`](../architecture.md) (**decision #18**, #17 marked superseded, *Phasing of Upstream Integrations* retargeted); [`./gaps.md`](./gaps.md) (G8.1 epic refs); [`./delivery-operating-model.md`](./delivery-operating-model.md); `delivery/dispatch-graph.yaml` (nodes renamed + moved to a `phase: 0-mock` block ahead of phase 0, header note rewritten, `epic-0.2`/`epic-0.3` edges); `delivery/ledger/epic-0.9.yaml` → `epic-0M.1.yaml`, `epic-0.10.yaml` → `epic-0M.2.yaml`; `delivery/ledger/epic-0.2.yaml`/`epic-0.3.yaml` notes; `delivery/README.md`, `delivery/ledger/README.md`; `scripts/python/build_html.py` (new NAV group, 2 stale entries removed); [SCP](../sprint-change-proposal-2026-08-18c.md) |
| **v4.6 — contract-shaped mock repos for both upstream sources; two new Phase 0 epics; 16 → 18 repos** | 2026-08-18 | **Decision #17** (SCP 2026-08-18b): neither upstream contract is confirmed (gaps.md **G8.1**, a wave-1 blocker), yet Epic 0.2 (nightly eLinks sync) and Epic 0.3 (weekly MRD workbook) cannot be built, demoed or negative-tested without an upstream to talk to. **Two new repos and two new epics**: **`ctam-joh-mock`** / **Epic 0.9** (2 stories — all 15 `jo_*` entities served as JSON in CTAM's expected eLinks contract shape, version-tagged ET-flavoured fixtures carrying the Employment Tribunals jurisdiction and its parent-child shape under Tribunals, fault-injection modes for malformed payload / per-record non-conformance / unreachable / slow) and **`ctam-mrd-mock`** / **Epic 0.10** (2 stories — MRD-shaped workbook generator publishing conformant, per-row-non-conformant, structurally-invalid, cross-sheet-lookup-failure and byte-identical-re-drop variants into a configurable blob container, Azurite in CI). Each is sequenced **before** the ingestion epic that consumes it; Epic 0.2 gains `epic-0.9` and Epic 0.3 gains `epic-0.10` in `depends_on`. This generalises the `ctam-mock-auth` mock-first pattern from identity to the upstream data plane — an external-team dependency becomes a **configuration cutover**. **New ARs AR55 / AR56**; **gaps.md G5.3 widened** to cover all three mocks (three guards each: `production`-profile refusal, `deploy-production.yml` exclusion, CI lint against production manifests); **G8.1 annotated as de-risked but explicitly NOT closed** — the mocks encode CTAM's written expectation, not evidence, so closure still requires real upstream data plus confirmed ET jurisdiction coverage. Neither mock owns a table in the shared schema (fixtures are in-repo files), so the Epic 0.1 fitness function needs no exception. **Numbering decision:** the epics were **appended as 0.9/0.10 rather than inserted at 0.2/0.3**, so none of the 23 existing stories renumbered — establishing explicitly that a Phase 0 epic number is an **identifier, not a build position**, and that `delivery/dispatch-graph.yaml` is the sole authority on order (verified acyclic; topological order `0.0 → arch-baseline → 0.1 → 0.9 → 0.2 → 0.10 → 0.3 → 0.4 → 0.5 → 0.6 → 0.7 → 0.8`). **Repos: 16 → 18. Phase 0: 9 → 11 epics, 23 → 27 stories.** **No code impact** (implementation not started; all Phase 0 epics remain `not-started`). | new [`../epics/phase-0/epic-0.9-joh-reference-data-mocked.md`](../epics/phase-0/epic-0.9-joh-reference-data-mocked.md) + new [`epic-0.10-mrd-reference-data-mocked.md`](../epics/phase-0/epic-0.10-mrd-reference-data-mocked.md); [`epic-0.2-joh-reference-data-ingested.md`](../epics/phase-0/epic-0.2-joh-reference-data-ingested.md) + [`epic-0.3-mrd-reference-data-ingested.md`](../epics/phase-0/epic-0.3-mrd-reference-data-ingested.md) (upstream stand-in sections; Story 0.2.3's WireMock/stub AC replaced; failure-path and G8.1-gate ACs retargeted); [`phase-0/index.md`](../epics/phase-0/index.md) (sequencing narrative, epic-number-vs-order note, scope model, epics table, summaries, demo table, retirement path); [`../epics/index.md`](../epics/index.md), [`../epics/framework.md`](../epics/framework.md) (new *Upstream Source Stand-Ins* Area), [`../epics/fr-coverage-map.md`](../epics/fr-coverage-map.md), [`../epics/requirements-inventory.md`](../epics/requirements-inventory.md) (AR55, AR56); [`../architecture.md`](../architecture.md) (decision #17, external-systems bullets, new *Phasing of Upstream Integrations* section, implementation sequence, structure alignment); [`../architecture-summary.md`](../architecture-summary.md) (non-production support table, open-decisions list); [`./repository-strategy.md`](./repository-strategy.md) (2 new rows, 16 → 18, rationale for mock-as-repo); [`./gaps.md`](./gaps.md) (G5.3 widened, G8.1 annotated); [`./data-tables.md`](./data-tables.md) (0-table mock section); [`./delivery-operating-model.md`](./delivery-operating-model.md); `delivery/dispatch-graph.yaml` (2 nodes, 2 `depends_on` edits, epic-number note); new `delivery/ledger/epic-0.9.yaml` + `epic-0.10.yaml`, notes on 0.2/0.3 shards; `delivery/README.md`, `delivery/ledger/README.md`; root `CLAUDE.md`; `scripts/python/build_html.py` NAV; [SCP](../sprint-change-proposal-2026-08-18b.md) |
| **v4.5 — Epic 0.5 filename aligned to the `joh` epic-slug convention** | 2026-08-18 | **Editorial/consistency change only — no architecture decision, no scope change.** `epics/phase-0/epic-0.5-reference-data-read-only-api.md` renamed to **`epic-0.5-joh-reference-data-read-only-api.md`**, so the read-API epic pair is symmetric with its ingestion counterparts and with each other: `epic-0.2-joh-reference-data-ingested` / `epic-0.3-mrd-reference-data-ingested` (ingestion) and `epic-0.5-joh-reference-data-read-only-api` / `epic-0.6-mrd-reference-data-read-only-api` (read APIs). Before the v4.4 split (SCP 2026-08-15c) there was one read-API epic and an unqualified slug was unambiguous; since the split, `epic-0.5-reference-data-read-only-api` was the only Phase 0 epic slug not naming its upstream source. **The epic number, title, `epic:` key, story IDs (0.5.1/0.5.2), FR/NFR coverage, dependency edges, and scope are all unchanged** — `delivery/ledger/epic-0.5.yaml` and `delivery/dispatch-graph.yaml` key on the epic number, not the slug, and are untouched. Note the epic still serves JOH tier-(a) **plus** the 15 tier-(b) CTAM-owned tables; the slug names the upstream source (as its siblings do), not the full tier coverage — the `title:` remains the authoritative scope statement. **No code impact** (implementation not started; Epic 0.5 `not-started`). Historical references to the old filename in [SCP 2026-08-15b](../sprint-change-proposal-2026-08-15b.md), [SCP 2026-08-15c](../sprint-change-proposal-2026-08-15c.md) and the v4.4 row below are preserved verbatim as audit trail and now point at the pre-rename path. | [`../epics/phase-0/epic-0.5-joh-reference-data-read-only-api.md`](../epics/phase-0/epic-0.5-joh-reference-data-read-only-api.md) (renamed; `resource:` front-matter updated); [`../epics/phase-0/index.md`](../epics/phase-0/index.md) (epics-table link + "Full epic with stories" link); [`../epics/fr-coverage-map.md`](../epics/fr-coverage-map.md) (FR6 ×2, FR7, FR58 link targets); `scripts/python/build_html.py` NAV; [SCP](../sprint-change-proposal-2026-08-18.md) |
| **v4.4 — Reference Data read API split into a JOH+tier-(b) surface and an MRD surface; Phase 0 renumbered again** | 2026-08-15 | **Decision #16** (SCP 2026-08-15c): **Epic 0.5** ("JOH and tier-(b) reference data served read-only...") is trimmed to serve JOH tier-(a) data + the 15 tier-(b) CTAM-owned tables only — none of which are MRD-sourced. **New Epic 0.6** ("MRD reference data served read-only...", 1 story) gives MRD Specialisations their own read-API surface: same deployable (`ctam-reference-data`), same `api-ctam-reference-data` OpenAPI artefact, a distinct resource group (`/v1/reference-data/mrd/...`) — mirroring decision #15's ingestion split at the read-API layer, and closing a pre-existing gap where the read API's AC never actually listed an MRD/Specialisations endpoint. **Phase 0 renumbered**: 0.6 bootstrap → **0.7** (story 0.6.1 → 0.7.1), 0.7 notification → **0.8** (stories 0.7.1/0.7.2 → 0.8.1/0.8.2). Dependency graph: Epic 0.5 drops its Epic 0.3 (MRD) dependency (`depends_on: [epic-0.1, epic-0.2, epic-0.4]`); new Epic 0.6 depends on `[epic-0.1, epic-0.3, epic-0.4]`; `future.phase-1-joh` and `future.post-mvp-admin-ui` now depend on both Epic 0.5 and Epic 0.6. Phase 0: 8 → 9 epics, 22 → 23 stories. **No code impact** (implementation not started). | [`../architecture.md`](../architecture.md) (new decision #16); [`../epics/phase-0/epic-0.5-reference-data-read-only-api.md`](../epics/phase-0/epic-0.5-reference-data-read-only-api.md) (trimmed to JOH+tier-b); new [`epic-0.6-mrd-reference-data-read-only-api.md`](../epics/phase-0/epic-0.6-mrd-reference-data-read-only-api.md); `epic-0.6-user-populations-bootstrapped.md` renamed to `epic-0.7-*`; `epic-0.7-system-dispatches-emails.md` renamed to `epic-0.8-*`; [`phase-0/index.md`](../epics/phase-0/index.md) rewritten; `delivery/ledger/` (epic-0.5 rewritten, new epic-0.6, epic-0.7/0.8 renamed+rewritten); `delivery/dispatch-graph.yaml` (full rewrite incl. `future:` dependency edges); [`../epics/index.md`](../epics/index.md), [`../epics/fr-coverage-map.md`](../epics/fr-coverage-map.md) (FR6/FR7/FR58 rows split JOH/tier-b vs MRD), [`../epics/framework.md`](../epics/framework.md); `delivery/README.md`; `scripts/python/build_html.py` NAV; [SCP](../sprint-change-proposal-2026-08-15c.md) |
| **v4.3 — Postgres schema design split into its own epic; JOH/MRD ingestion split into two epics; Phase 0 renumbered** | 2026-08-15 | **Decision #15** (SCP 2026-08-15b): a **new Epic 0.1** ("Shared PostgreSQL schema design is established and CI-enforced," 2 stories) finalizes the relational schema in `architecture/data-tables.md` and builds a CI fitness function (naming/PK/FK/timestamp/tier-prefix conventions) **before** any table-creating epic. **The former combined JOH+MRD ingestion epic splits into two independently-demoable epics**: JOH eLinks (JSON, nightly, 3 stories) and MRD Excel (weekly Blob drop, 1 story) differ in format, cadence, and delivery mechanism. **Phase 0 is fully renumbered**: 0.0 platform estate (unchanged, 6 stories) → **0.1 schema design (new, 2 stories)** → **0.2 JOH ingestion** (was 0.1, trimmed to 3 stories: 0.2.1–0.2.3) → **0.3 MRD ingestion (new, 1 story: 0.3.1, was 0.1's Story 0.1.4)** → **0.4 auth** (was 0.2, 5 stories renumbered 0.4.1–0.4.5) → **0.5 read API** (was 0.3, 2 stories renumbered 0.5.1–0.5.2; now depends on both Epic 0.2 and Epic 0.3 for tier-(a) data instead of one bundled ingestion epic) → **0.6 bootstrap** (was 0.4, story renumbered 0.6.1) → **0.7 notification** (was 0.5, 2 stories renumbered 0.7.1–0.7.2). Every epic markdown file, ledger shard, and the dispatch graph (`delivery/dispatch-graph.yaml`) renamed/renumbered accordingly, including dependency edges (e.g. Epic 0.5 now depends on `[epic-0.1, epic-0.2, epic-0.3, epic-0.4]`). Phase 0: 6 → 8 epics, 20 → 22 stories. **No code impact** (implementation not started; all Phase 0 epics remain `not-started` in the ledger). | [`../architecture.md`](../architecture.md) (new decision #15); [`../epics/phase-0/`](../epics/phase-0/) (all 8 epic files — 2 new: `epic-0.1-postgres-schema-design.md`, `epic-0.3-mrd-reference-data-ingested.md`; `epic-0.1-upstream-reference-data-ingested.md` renamed to `epic-0.2-joh-reference-data-ingested.md`; `epic-0.2/0.3/0.4/0.5` files each renamed +2 and renumbered internally; [`phase-0/index.md`](../epics/phase-0/index.md) rewritten); `delivery/ledger/` (epic-0.1..0.7 shards renamed/rewritten); `delivery/dispatch-graph.yaml` (full rewrite incl. `future:` dependency edges); [`../epics/index.md`](../epics/index.md), [`../epics/fr-coverage-map.md`](../epics/fr-coverage-map.md) (every FR row's epic/story references), [`../epics/framework.md`](../epics/framework.md), [`../epics/requirements-inventory.md`](../epics/requirements-inventory.md); [`./gaps.md`](./gaps.md) (G10.2 reference), [`./repository-strategy.md`](./repository-strategy.md) (MRD storage reference); `delivery/README.md`; [SCP](../sprint-change-proposal-2026-08-15b.md) |
| **v4.2 — network isolation + edge protections added to Epic 0.0**| 2026-08-14 | **Decision #14** (SCP 2026-08-14): a new **Story 0.0.6** (Epic 0.0) hardens the shared estate after Stories 0.0.2–0.0.5 — NSGs on every AKS subnet, private-endpoint-only access for PostgreSQL/Key Vault/ACR (public network access disabled), and a WAF (OWASP CRS, Prevention mode) + DDoS Protection plan at the APIM edge. Closes a previously-undocumented gap between **NFR15** (GovS7 alignment) and Epic 0.0's acceptance criteria, which exercised TLS/encryption-at-rest/secret-management (NFR10/NFR11/NFR16) but no network-perimeter control. **New AR54**; **new gap G10** (G10.1 DDoS tier + WAF policy ownership pending HMCTS confirmation; G10.2 cross-repo private-DNS-zone conventions, same class as G9.1). Story 0.0.5's "all five layers" capstone AC moves to Story 0.0.6 as "all six layers." Phase 0: 6 epics / 19 → **20 stories** (Epic 0.0: 5 → 6 stories). Considered and explicitly deferred: IaC/container vulnerability scanning, Defender for Cloud, Azure Policy/CIS baseline (candidates for a future SCP). **No code impact** — implementation not started (all 6 Phase 0 epics `not-started`). | [`../architecture.md`](../architecture.md) (new decision #14); [`./gaps.md`](./gaps.md) (new **G10**, title `G1–G9` → `G1–G10`); [`../epics/requirements-inventory.md`](../epics/requirements-inventory.md) (new **AR54**); [`../epics/phase-0/epic-0.0-platform-estate-provisioned.md`](../epics/phase-0/epic-0.0-platform-estate-provisioned.md) (new Story 0.0.6; `storyCount` 5→6; Key NFRs/Architecture requirements lines; Story 0.0.5 capstone AC moved); `delivery/ledger/epic-0.0.yaml` (story `0.0.6`); `delivery/dispatch-graph.yaml` (`epic-0.0.stories` gains `0.0.6`); [`../epics/phase-0/index.md`](../epics/phase-0/index.md), [`../epics/index.md`](../epics/index.md) (story-count propagation); `delivery/README.md` (current-state count); [SCP](../sprint-change-proposal-2026-08-14.md) |
| **v4.1 — programme renamed RAM → CTAM (*Court and Tribunals Availability Management*)** | 2026-08-13 | **D14** (SCP 2026-08-13): the programme name **RAM** (*Resource Availability/Allocation Management*) is retired in favour of **CTAM** (*Court and Tribunals Availability Management*). **Naming only — zero semantic content.** The 16-repo decomposition, the 55-table data model, all 60 FRs / 42 NFRs, the Phase 0–8 build sequence, all 6 Phase 0 epics / 19 stories, `dispatch-graph.yaml`, `delivery/ledger/` and decisions D1–D13 are **unchanged**; only the tokens naming them differ. <br/>**Surfaces renamed:** product codename `RAM Pathfinder` → **`CTAM Pathfinder`**; the 17 repository names `ram-*` → **`ctam-*`** (incl. the control plane, `ram-analysis` → `ctam-analysis`, already renamed on GitHub); the tier-(b) owned-table prefix `ram_` → **`ctam_`** and index names `uq_ram_*` → `uq_ctam_*`; the Java root package `uk.gov.hmcts.ram` → **`uk.gov.hmcts.ctam`** (and `src/…/uk/gov/hmcts/ram/` source paths); the API-spec artefact coordinates `api-ram-{service}` → **`api-ctam-{service}`**; the shared database schema `ram` → **`ctam`**; the DNS hostnames `ram.hmcts.gov.uk` / `api.ram.{env}.hmcts.gov.uk` / `admin.ram.hmcts.gov.uk` → their `ctam` equivalents; and adjectival prose (`RAM-owned` / `RAM-assigned` / `RAM-internal` / `RAM-overlay`). ~2,780 occurrences across 88 files, plus 80 regenerated files under `docs/`. <br/>**Deliberately NOT renamed:** the upstream tier-(a) prefixes **`jo_` and `mrd_`** (they name JOH eLinks and MRD, not this programme — the two-tier ownership contract is untouched) and the dev-only **`mock_`** prefix. Boundary-anchored patterns protected `framework`, `parameter`, `diagram`, `reframed`, `FedRAMP` and `Ramnish` from the sweep. <br/>**Two paths renamed on disk:** `briefs/brief-ram-analysis-2026-08-09/` → `brief-ctam-analysis-2026-08-09/`; `.claude/memory/project_bmad_ram_pathfinder_state.md` → `project_bmad_ctam_pathfinder_state.md`. <br/>**Convention override (scoped to this change):** dated reports, prior SCPs and pre-v4.1 changelog entries were **rewritten in place** rather than preserved verbatim, so the artifact set is uniformly searchable under one name — git holds the point-in-time record. <br/>**No code impact** — implementation not started (all 6 Phase 0 epics `not-started`), so no migration, deprecation window or dual-naming period is required. | Every artifact in the pack, by token sweep. Substantive additions: [`../prd.md`](../prd.md) (**new D14**, new **CTAM** glossary entry, `[^d14]` footnote, `editHistory`); this changelog; [SCP](../sprint-change-proposal-2026-08-13.md); `build_html.py` NAV; `docs/` regenerated |
| **v4.0 — wave-1 pilot retargets SSCS → Employment Tribunals (ET); SSCS to wave 2; Courts to waves 3+** | 2026-08-07 | **D13 supersedes D11 for wave ordering** (SCP 2026-08-07): wave 1 = the **Employment Tribunals (ET)** jurisdiction; wave 2 = **SSCS** (replacing ListAssist; GAPS retained); waves 3+ = Courts jurisdictions per HMCTS judicial region (replacing APEX/JI). <br/>**Structurally a no-op** — D8 already made jurisdiction a first-class attribute *sourced from upstream `jo_jurisdictions`*, so retargeting the pilot is a data-and-rollout change, not an architectural one. **Unchanged:** the 16-repo decomposition, the 55-table data model, all 60 FRs / 42 NFRs (none added, removed or renumbered; ~12 reworded where their prose names a jurisdiction or incumbent), the Phase 0–8 build sequence, all 6 Phase 0 epics / 19 stories, `dispatch-graph.yaml`, `delivery/ledger/`, and decisions D1–D4, D6, D7, D9, D10, D12. <br/>**Wave renumbering** — ~100 lines carrying "waves 2+ (Courts)" become "waves 3+" across 24 files. <br/>**ET's scheduling incumbent is UNIDENTIFIED** — recorded throughout as the placeholder token `[ET-INCUMBENT-TBD]` so resolution is one mechanical sweep, not a re-analysis. **Nothing asserts what ET's incumbent is.** <br/>**New gaps:** **G8.4** (ET incumbent unidentified — load-bearing across D5, FR60/NFR41, NFR36, NFR32, G8.3 and the business case); **G8.5** (ET JOH taxonomy / role set unconfirmed — the **ET as-is analysis pack** is a new wave-1 deliverable; every ET role name in the artifact set is marked *provisional*); **G8.6** (**JFEPS/Liberata applicability to ET unverified** — the 2026-06-11 SSCS verification does not transfer; if ET pays by another route, NFR21 weakens and Phase 6 gains wave-1 risk). **G8.1 promoted to a wave-1 blocker** (eLinks `jo_jurisdictions` must carry ET); **G8.3 retargeted**. <br/>**PRD:** D13 added; Target Users gains a provisional ET set with the SSCS set preserved verbatim at wave 2; Journey 1 rewritten as the ET wave-1 journey (**structural placeholder** — chain settled, ET specifics provisional) with the SSCS journey kept as a wave-2 variant (journey count stays 6); 5 new glossary entries. <br/>**Business case** retargeted to ET as a **settled programme decision**, with two supporting-evidence items flagged inline as outstanding (the wave-1 replacement driver and ET's quantified risk profile — both originally assessed for SSCS; both land with the ET as-is analysis pack). <br/>**No code impact** — implementation not started (all 6 Phase 0 epics `not-started`). | [`../prd.md`](../prd.md) (D5, D8, D11 marked superseded, **new D13**, Exec Summary, Target Users, Journey 1, Success Criteria, Scope, Phase mapping, Integration table, FR60, NFR21/24/32/36/38/41, Glossary); [`../business-case.md`](../business-case.md) (retargeted; 2 evidence items flagged); [`../architecture.md`](../architecture.md); [`../architecture-summary.md`](../architecture-summary.md); [`./gaps.md`](./gaps.md) (G8.4–G8.6 new; G8.1/G8.3 amended); [`./conventions.md`](./conventions.md); [`./data-tables.md`](./data-tables.md); [`./user-types.md`](./user-types.md); [`./repo-structure.md`](./repo-structure.md); [`./repository-strategy.md`](./repository-strategy.md); [`./functional-requirements-coverage.md`](./functional-requirements-coverage.md); [`./non-functional-requirements-coverage.md`](./non-functional-requirements-coverage.md); sequence diagrams ([payment-batch-flow](./sequence-diagrams/payment-batch-flow.md) — ⚠ G8.6 notice, [joh-onboarding](./sequence-diagrams/joh-onboarding-and-sitting-generation.md)); epics ([index](../epics/index.md), [framework](../epics/framework.md), [fr-coverage-map](../epics/fr-coverage-map.md), [requirements-inventory](../epics/requirements-inventory.md), [phase-0/index](../epics/phase-0/index.md), [0.2](../epics/phase-0/epic-0.2-user-authenticates.md)/[0.3](../epics/phase-0/epic-0.3-reference-data-read-only-api.md)/[0.4](../epics/phase-0/epic-0.4-user-populations-bootstrapped.md) AC lines); `CLAUDE.md`; `README.md`; `project-context.md`; `build_html.py` NAV; [SCP](../sprint-change-proposal-2026-08-07.md) |
| **v3.9 — CTAM-assigned JOH identity (`ctam_joh_identities`); `personnel_number` demoted to upstream link** | 2026-07-09 | **Identity model (refines D9)**: `personnel_number` is no longer the canonical JOH identifier. CTAM mints a stable **`ctam_joh_identities`** UUID (`joh_id`) referenced by every domain table; `personnel_number` is retained only as the upstream link to `jo_people`, insulating CTAM domain data from upstream churn. <br/>**New table** `ctam_joh_identities` (owned by `ctam-reference-data`; `id uuid PK` + unique `personnel_number`; minted **eagerly** during the eLinks sync) — reference-data table count 32 → 33. <br/>**FK convention flips**: domain tables FK `joh_id` → `ctam_joh_identities`, never `personnel_number`. <br/>**API**: JOH resources rekeyed `/v1/johs/{personnelNumber}` → `/v1/johs/{johId}` (`personnel_number` becomes a `?personnelNumber=` filter). <br/>**Auth**: both populations now resolve to a CTAM-assigned UUID; `/authz/check` `canonicalId` is the CTAM JOH UUID. **No code impact** — implementation not started. | [`./conventions.md`](./conventions.md); [`./data-tables.md`](./data-tables.md); [`./repository-strategy.md`](./repository-strategy.md); [`./functional-requirements-coverage.md`](./functional-requirements-coverage.md); [`./user-types.md`](./user-types.md); [`./assumptions.md`](./assumptions.md); sequence diagrams (auth, joh-onboarding); [`../architecture.md`](../architecture.md); [`../architecture-summary.md`](../architecture-summary.md); [`../prd.md`](../prd.md); epics ([`requirements-inventory.md`](../epics/requirements-inventory.md), [`framework.md`](../epics/framework.md), [`fr-coverage-map.md`](../epics/fr-coverage-map.md), [`phase-0/index.md`](../epics/phase-0/index.md), [0.1](../epics/phase-0/epic-0.1-upstream-reference-data-ingested.md)/[0.2](../epics/phase-0/epic-0.2-user-authenticates.md)/[0.4](../epics/phase-0/epic-0.4-user-populations-bootstrapped.md)); `project-context.md`; [SCP](../sprint-change-proposal-2026-07-09.md) |
| **v3.8 — shared infrastructure to dedicated `ctam-shared-infrastructure` repo (CNP)** | 2026-07-06 | Aligns with the HMCTS Cloud Native Platform `{product}-shared-infrastructure` standard. The shared Azure estate (AKS, PostgreSQL, ACR, APIM, App Insights, Key Vault) moves **out of `ctam-reference-data`** into a new dedicated **`ctam-shared-infrastructure`** repo (16 repos total); provisioned + **independently verified** in the new **Epic 0.0** (5 stories, each a Terraform layer with a deploy-time acceptance test). **AR53 inverted** (dedicated repo — supersedes the "colocated first-consumer" rule of v3.1/#12). Story 0.1.1 reduced to scaffold-and-deploy-onto-estate. Phase 0: 5 → 6 epics, 14 → 19 stories. Decision #13. **No code impact** — implementation not started. | [`../architecture.md` decision #13](../architecture.md); [`./repository-strategy.md` repo list 15 → 16](./repository-strategy.md); [`./repo-structure.md` per-service terraform note + new shared-infra tree](./repo-structure.md); [`../epics/requirements-inventory.md` AR53 + AR23/AR27](../epics/requirements-inventory.md); [`../epics/framework.md`](../epics/framework.md); epics [`index.md`](../epics/index.md), [`phase-0/index.md`](../epics/phase-0/index.md), new [`epic-0.0-platform-estate-provisioned.md`](../epics/phase-0/epic-0.0-platform-estate-provisioned.md), [`epic-0.1` Story 0.1.1](../epics/phase-0/epic-0.1-upstream-reference-data-ingested.md); [SCP](../sprint-change-proposal-2026-07-06.md) |
| **v3.7 — SSCS wave 1 replaces ListAssist, not GAPS (GAPS retained)** | 2026-06-18 | Correction: in the SSCS pilot (wave 1) CTAM Pathfinder replaces **ListAssist** (the SSCS judicial-scheduling tool); **GAPS, the SSCS case-management system, is retained** (not decommissioned) — analogous to the D12 external case-management systems that consume CTAM's APIs. All SSCS-wave-1 scheduling-incumbent references (replacement target, manual-UAT parity reference, per-wave rollback target, historical-scheduling-data location, "incumbent-experienced users") change GAPS → ListAssist; GAPS is reframed as the retained SSCS case-management system in the glossary and the external-systems lists. Amends D11 and the [^d11] footnote. | PRD (D11, glossary, Exec Summary, Success Criteria, Scope, Journey 1, FR60, NFR32/36/41); architecture.md + summary; conventions; FR/NFR coverage; user-types; epics (framework + [^d11] footnote across files) |
| **v3.6 — DB migration tool: Flyway → Liquibase** | 2026-06-18 | **Liquibase replaces Flyway** as the schema-migration tool across the architecture pack, epics/stories, and PRD FR8. Per-service changelogs under `src/main/resources/db/changelog/` (master `db.changelog-master.yaml`; formatted-SQL changesets `NNN-name.sql`); `ctam-architecture` owns the shared `ctam_configuration_values` baseline changelog. Liquibase is a **CTAM convention** — the HMCTS demo repo's Database branch uses Flyway; CTAM standardises on Liquibase (AR19). Reverses the v1.4 "Liquibase → Flyway" alignment. | AR19, FR8 (PRD + mirror), A29, G1.4/G1.4a; `architecture.md` Data Architecture; `conventions.md`; `repo-structure.md`; `data-tables.md`; `starter-template.md` §B; `architecture-summary.md`; all Phase 0 epics |
| **v3.5 — HMCTS Crime SpringBoot template reconciled against `main`** | 2026-06-17 | Reviewed the scaffolding assumptions against the live `hmcts/service-hmcts-crime-springboot-template`@`main`. **Corrected (factual):** scaffold clone URL `hmcts/spring-boot-template` → `hmcts/service-hmcts-crime-springboot-template` (AR2, starter-template, epic Stories 0.1.1/0.2.1); Spring Boot `4.0.6` → **`4.1.0`**; `gradle-git-properties` `2.5.7` → **`4.0.1`**; `spring-boot-testcontainers` `4.0.6` → **`4.1.0`** (tracks the Spring Boot BOM). **Confirmed aligned:** Java 25, Groovy DSL, `io.spring.dependency-management:1.1.7`, Lombok 1.18.46, `cyclonedx.bom:3.2.4`, `ben-manes.versions:0.54.0`, `logstash-logback-encoder:9.0`, OpenTelemetry, `spring-boot-starter-webmvc-test`, JaCoCo; Helm + Key Vault correctly noted as non-baseline (G1.4a/b). **Open finding (pending reframe — NOT yet applied):** the base template is deliberately **minimal** (actuator/observability/logging only); Flyway, Testcontainers, MapStruct, JJWT/JWTFilter, OWASP encoder, the docker-compose plugin, and OpenAPI tooling are **not in the baseline** — they are patterns in the separate `hmcts/service-hmcts-springboot-demo` repo. G1.4/A4/A29 overstate the baseline; the CTAM scaffolding script's scope is larger than G1.4a/b captured. See the readiness/scaffolding review note. | [`./starter-template.md`](./starter-template.md); [`../architecture.md` stack line](../architecture.md); [`../epics/requirements-inventory.md` AR2/AR5/AR11/AR14](../epics/requirements-inventory.md); epic Stories 0.1.1 / 0.2.1; **pending:** G1.4/A4/A29 reframe |
| **v3.4 — integrations-first Phase 0** | 2026-06-17 | Phase 0 re-sequenced so ingestion (JOH eLinks + MRD) is the first epic. `ctam-reference-data` is scaffolded first and carries the shared-estate Terraform (relocated from `ctam-authorisation`, AR53). Ingestion stays in-process — no `ctam-integrations` repo. Reference Data read API follows the auth epic. Phase 0: 4 → 5 epics, 12 → 14 stories. Decision #12. | [`../architecture.md`](../architecture.md); [SCP](../sprint-change-proposal-2026-06-17.md) |
| **v3.3 — system-context diagram re-authored in D2 + ELK** | 2026-06-12 | **Diagram tooling decision**: new boxes-and-lines architecture diagrams are authored in **D2 with the ELK layout engine** (`brew install d2`) — orthogonal, obstacle-avoiding edge routing with labels anchored to edges, which Graphviz's `ortho` mode cannot do. The system-context diagram is re-authored as `diagrams/system-context.d2` (canonical source for `system-context.png`); the old `system-context.dot` is removed (2026-06-12 hygiene pass — git history preserves it). Externals are placed adjacent to the service each talks to (the single far-away externals box was what forced long criss-crossing edges); the case-management edge points in the data direction (APIM → consumer). `scripts/render_diagram.sh` renders `.d2` inputs via the d2 CLI; Graphviz remains the right tool for the record/table-style as-is schema renders; Mermaid remains for sequence diagrams. | [`./diagrams/system-context.d2` (new canonical source)](./diagrams/system-context.d2); `./diagrams/system-context.dot` (removed — see git history); `scripts/render_diagram.sh` (.d2 support) |
| **v3.2 — `ctam_` prefix on every CTAM-owned table; `_overlays` suffix pattern retired** | 2026-06-11 | **Naming rule simplified (product decision): ownership is in the prefix.** Every CTAM-owned table now carries the **`ctam_` prefix** — domain tables (`ctam_bookings`, `ctam_absences`, `ctam_vacancies`, `ctam_sittings`, `ctam_payments`, …), tier-(b) reference data (`ctam_regions`, `ctam_offices`, `ctam_calendar_periods` + 12 vocabularies), auth tables (`ctam_auth_users`, `ctam_auth_staff_identities`, …), JOH operational state, and `ctam_configuration_values`. Upstream-sourced tier-(a) tables keep their source-system prefix (`jo_`, `mrd_`) — the prefix now tells you the owner at a glance. **`_overlays` suffix retired**: `joh_ticket_overlays` → **`ctam_joh_ticket`**; `joh_location_overlays` → **`ctam_joh_location`**. **`jo_sync_status` → `ctam_sync_status`** — it is CTAM-internal (PRD revised D3 says so explicitly); the old `jo_` prefix wrongly signalled tier (a). Dev-only mock-auth tables keep `mock_` (never deployed to production; documented exemption). Constraint names follow their tables (`uq_ctam_bookings_…`, `uq_ctam_payments_…`). Stale judge-keyed API/code examples in conventions corrected in passing (`/v1/judges/{judgeId}` → `/v1/johs/{personnelNumber}`). | [`../architecture.md` *Data Architecture* (naming rules, grant examples)](../architecture.md); [`../architecture-summary.md`](../architecture-summary.md); [`./data-tables.md` (full inventory renamed)](./data-tables.md); [`./conventions.md` Naming Patterns (rule rewritten) + Pattern Examples](./conventions.md); [`./gaps.md` G6.4 grant example](./gaps.md); [`./assumptions.md`](./assumptions.md); FR/NFR coverage shards; sequence diagrams (.md + .mmd, PNGs regenerated); `./diagrams/system-context.dot` (PNG regenerated); PRD D3/D9/D10/FR8/FR57 table mentions; epics pack (requirements-inventory, framework, fr-coverage-map, Phase 0 epics) |
| **v3.1 — Terraform mandated for all infrastructure provisioning; colocated first-consumer ownership** | 2026-06-11 | **HMCTS standard adopted**: all Azure infrastructure is provisioned via **Terraform** — no Bicep (the prior Story 0.1.1 "Terraform / Bicep, recommendation Bicep" wording is corrected), no portal click-ops. **Colocation rule** (product decision): Terraform lives in the **first repo that needs the resource** — infra cannot live separate from the application that needs it. Allocation: shared estate (AKS, PostgreSQL Flexible Server, ACR, APIM instance + base policies, App Insights/Log Analytics incl. retention) in `ctam-authorisation`'s `terraform/` (first scaffolded service); per-service resources (Key Vault namespaces, APIM per-API policies) in each service repo; MRD storage in `ctam-reference-data`; Static Web App in `ctam-ui`. Per-environment stacks (`dev`/`staging`/`production`); `ctam-scaffold.sh` adds the `terraform/` skeleton. **Division of labour**: Terraform provisions the estate; Helm deploys workloads; Flyway owns DB schema. **New gap G9.1**: state backend + plan/apply pipeline + cross-repo remote-state conventions to confirm with HMCTS before the Story 0.1.1 estate apply. | [`../architecture.md` *Infrastructure & Deployment* (new provisioning statement); *Implementation Sequence* item 1; *First implementation steps* item 1](../architecture.md); [`../architecture-summary.md` *Deployment topology*](../architecture-summary.md); [`./gaps.md` G1.4a (Bicep speculation removed), G3.6(e) (DR stack Terraform-defined), new G9](./gaps.md); [`./assumptions.md` A20, A34](./assumptions.md); [`./repo-structure.md` per-service tree gains `terraform/`](./repo-structure.md); epics: [`requirements-inventory.md` new AR53 + AR23/AR25/AR27 touch-ups](../epics/requirements-inventory.md), [`epic-0.1` Stories 0.1.1 (estate AC + Bicep fix), 0.1.3 (terraform skeleton), 0.1.4 (MRD storage via Terraform), 0.1.6 (Static Web App via Terraform)](../epics/phase-0/epic-0.1-user-authenticates.md), [`framework.md` Platform & DevEx scope](../epics/framework.md) |
| **v3.0 — Sprint Change Proposal 2026-06-10 cascade: SSCS-first wave, no legacy migration, two-tier reference data, two-population identity, jurisdiction dimension, D12 scope boundary, `ctam-judge` → `ctam-joh`** | 2026-06-11 | **SSCS-first pilot wave (D11)** — wave 1 targets the SSCS Tribunals jurisdiction, replacing GAPS; APEX/JI continues for Courts until waves 2+. The 11-service decomposition and Phase 0–8 build sequence are unchanged. <br/>**No legacy migration (revised D3)** — the Phase 0 Data Migration ETL is **retracted**; `ctam-architecture/migration/` is no longer a deliverable. Reference data is ingested from upstream sources of truth: **JOH eLinks API** (15 `jo_*` entities; nightly in-process `@Scheduled` sync inside `ctam-reference-data`; full-refresh upsert on upstream natural keys; soft-deactivation, never hard-delete; state in `jo_sync_status`) + **MRD** (weekly Excel via Azure Blob drop + scheduled pick-up, transitional until MRD APIs ship). <br/>**Two-tier reference-data ownership (FR6/FR7)** — tier (a) upstream-sourced `jo_*`/`mrd_*` tables, read-only in CTAM, corrections at source, only `ctam_reference_data` holds INSERT/UPDATE; tier (b) CTAM-owned tables, DBA-via-SQL in MVP per D10. Table inventory restructured: 37 → 55 tables. <br/>**Two-population identity (restructured D9)** — JOH users resolve IdP email → `jo_people` → **personnel number** (canonical JOH identifier; domain tables now FK `personnel_number` → `jo_people`); HMCTS admin staff resolve via new **`auth_staff_identities`** table → CTAM-assigned UUID (architecture decision). The APEX ⇄ IdP ETL matching scheme (old TBD #7) is reframed as runtime identity resolution at sign-in. <br/>**Jurisdiction as first-class hierarchical dimension (reframed D8)** — sourced from `jo_jurisdictions`; carried by Authorisation (roles + jurisdiction + Region/Area scope per FR2); reference-data API responses jurisdiction-filtered; `auth_user_activation_flags` keyed by (jurisdiction, region) (FR57); rollout is jurisdiction-first, then per-region. <br/>**D12 scope boundary** — CTAM is the system of record for JOH availability/scheduling only; case management, panel composition, and hearing types live in external systems that consume CTAM's APIs (new external-integration row); no external system writes into CTAM. <br/>**`ctam-judge` → `ctam-joh` full rename** (D11 follow-up; architecture decision) — service, repo, DB role `ctam_joh`, table reshape: `judges` table retired (canonical person record is `jo_people`); `ctam-joh` keeps the CTAM-owned operational overlays keyed by `personnel_number` (`working_patterns`, `working_pattern_days`, `joh_ticket_overlays` — was `judge_tickets`, new `joh_location_overlays` per FR17, `jurisdictional_splits`). Vocabulary renames: `judge_types` → `joh_types`, `judge_fee_entitlements` → `joh_fee_entitlements`. <br/>**FR renumbering** — Phase 0 ETL FR retracted; FR58–FR61 → FR57–FR60 (61 → 60 FRs); all architecture references re-anchored (activation flags FR57; API-as-Product FR58; structured logging FR59; manual UAT FR60). NFR24 flipped: JOH eLinks + MRD are MVP integrations. <br/>**UAT reframed (D5/FR60)** — jurisdiction-incumbent-experienced users per wave (GAPS users wave 1; APEX users waves 2+). <br/>**D10 reconciliation** — `ctam-admin-ui` and admin-write APIs marked post-MVP throughout (the 2026-05-15 change had not been carried into the architecture pack). <br/>**New architecture-phase decisions resolved**: eLinks sync mechanism, MRD ingestion mechanism, staff canonical identifier, rename — recorded in `architecture.md` → *Architecture-phase decisions*. **New gap group G8** (JOH eLinks contract + MRD feed arrangements; upstream-overlap vocabulary candidates). A32/A33 superseded; A36/A37 added. **Wave-1 gate**: SSCS-cohort readiness assessment (D11) required before the Phase 9 cutover plan is finalised. | [`../architecture.md` — frontmatter; Requirements Overview; Technical Constraints; Cross-Cutting Concerns; architecture-phase decisions table (+4 new); Data Architecture (two-tier model + ingestion replaces ETL; FK/role renames); Authentication & Security (two-population identity resolution; bootstrap verification); Frontend Architecture (D10); Infrastructure (historical access per incumbent); Deployment topology (jurisdiction rollout boundary); Implementation Sequence; TBDs Resolved (#6, #7 reframed); Boundaries; Requirements-to-Structure; Cross-Cutting File Locations; Integration Points (internal + external); Data Flow (journey renumber); Wave rollout flow; Validation Results; Implementation Handoff](../architecture.md); [`../architecture-summary.md` — full sweep of the same changes](../architecture-summary.md); [`./data-tables.md` — restructured for the two-tier model; 55-table inventory](./data-tables.md); [`./gaps.md` — G4.6/G4.7 retired; G1.3 reworded; G7.1 trigger (d) removed; new G8](./gaps.md); [`./assumptions.md` — A32/A33 superseded; A36/A37 added](./assumptions.md); PRD (amended separately in the Correct Course run, 2026-06-10); SCP: [`../sprint-change-proposal-2026-06-10.md`](../sprint-change-proposal-2026-06-10.md) |
| **v2.11 — Retire the RFC process; post-verification sitting amendments handled via UI re-open + RBAC** | 2026-05-11 | **Process change**: the as-is APEX "Request for Change (RFC)" external workflow for amending verified sittings is **retired in CTAM Pathfinder**. Post-verification amendments are handled inside the UI by a **re-open action gated by RBAC**, with mandatory in-form justification and full audit. The `sitting:re-open` permission is granted to **RSU Admin only at MVP** — distinct from the original confirmer (SIT-NFR-02 extended) and from a standard Verifier. **API rename**: `POST /v1/sittings/rfc-unlock` → `POST /v1/sittings/{id}/re-open` (body carries the justification). **Glossary**: PRD `RFC` entry removed. **Why**: in the as-is system, RFC was an out-of-system ticketing workflow that gated who could request and who could approve. CTAM Pathfinder's first-class RBAC model (FR2) and per-request `authz/check` make that ticketing unnecessary — the same gate is achievable inside the UI with a permission, a justification field, and an audit row. Reduces friction and brings amendment handling into the same trace as every other write. | [PRD `FR40` reworded; PRD glossary `RFC` entry removed](../prd.md); [`./functional-requirements-coverage.md` FR40](./functional-requirements-coverage.md); [`./repository-strategy.md` `ctam-sitting` row](./repository-strategy.md); [`./sequence-diagrams/salaried-sitting-confirmation.md` narrative + Phase 4 summary row + "Not in this diagram" entry](./sequence-diagrams/salaried-sitting-confirmation.md); [`./sequence-diagrams/salaried-sitting-confirmation.mmd` Phase 4 redrawn — actor changed from Programme Admin to RSU Admin; endpoint renamed; justification + audit captured (PNG regenerated)](./sequence-diagrams/salaried-sitting-confirmation.mmd); epics.md FR40 *(revised)* + Phase 5 area scope updated |
| **v2.10 — Split UI into two repos: `ctam-ui` (business) and `ctam-admin-ui` (admin)** | 2026-05-11 | **Frontend boundary change** — the single `ctam-ui` SPA is split into two independently-deployed SPAs separated by audience: `ctam-ui` (business-user-facing: Judge, Absence, Vacancy, Booking, Sitting, Payment, Itinerary, Reports + business Home shell per FR55) and `ctam-admin-ui` (admin-facing: Reference Data maintenance per FR6, User & Role admin per FR4). Same stack and conventions for both (React + TypeScript + Vite + Vitest + Playwright + GOV.UK Design System + OIDC + RFC 9457 error handling), but distinct repos, CI/CD, CODEOWNERS, deployments, and hostnames. **Polyrepo count: 14 → 15.** Future admin surfaces reserved as module placeholders in `ctam-admin-ui`: activation flag dashboard (FR58 admin), migration reconciliation viewer (FR57), post-MVP audit viewer (D7 roadmap). **Why**: admin work has a different audience (system administrators, not RSU/Court/Judges), different change cadence (lower, higher stakes), and different review needs; separating at the repo boundary prevents accidental nav leakage of admin-only screens into business users' UI and gives independent rollout. Consistent with the backend's per-service polyrepo discipline: minimise shared code, accept duplication, gain independence. No backend changes; admin gating is enforced server-side at `ctam-authorisation` via role assignment, with the repo-level boundary as defence-in-depth. | [`../architecture.md` Step 4 *Frontend Architecture* (split into two-repos preamble + per-repo callouts + independent-deployment paragraph); Step 6 *UI Boundaries* (single SPA → two SPAs); Step 6 *Integration Points — Internal* (ASCII diagram updated to show both UIs); Step 6 *Requirements to Structure Mapping* (FR4 + FR6 rows annotated with admin UI location); Step 4 *Decision Impact Analysis — Implementation Sequence* (Phase 0 UI shell → two shells); Step 7 *Validation Strengths* (14 → 15 repos)](../architecture.md); [`./repository-strategy.md` Polyrepo intro (14 → 15 repos); Repository List (new `ctam-admin-ui` row + `ctam-ui` description tightened to business-only); 14-repo total → 15-repo total; new "Why split admin and business UI" paragraph](./repository-strategy.md); [`./repo-structure.md` "Complete Project Directory Structure — UI repo" → "UI repos" with `ctam-ui` (business) sub-section + new `ctam-admin-ui` (admin) sub-section showing admin module structure + future-surface placeholders](./repo-structure.md); [`./functional-requirements-coverage.md` FR1–FR5 architectural support (FR4 admin UI annotated) + FR6–FR9 architectural support (FR6 admin UI annotated)](./functional-requirements-coverage.md); epics.md AR1 *(repo count revised)*, AR42–AR45 *(revised to describe both repos)*, AR45b *(new — independent deployment)*; Phase × Area summary (UI Foundation row split into Business UI Foundation + Admin UI Foundation); Phase 0 areas (Reference Data scope clarifies admin UI lives in `ctam-admin-ui`; UI Foundation renamed to "Business UI Foundation"; new "Admin UI Foundation" area added) | 
| **v2.9 — DB-level detail consolidated to Data Architecture; vocabulary renames for clarity; HTML build + mock-data tooling added** | 2026-05-11 | **Database detail out of architecture docs** — column-level references, SQL operations (`SELECT ... FOR UPDATE`, `INSERT`, `UPDATE filled=true`, `gen_random_uuid()`), DDL fragments, and process-on-DB implications were stripped from every non-data section. The full retry-safety mechanism (natural-key uniqueness, `@Version` optimistic locking, `@Lock(PESSIMISTIC_WRITE)` pessimistic row locking, per-entity convention, `Idempotency-Key` policy, audit-trail boundary) now lives in a new **Retry safety and concurrency control** subsection inside `architecture.md` → *Data Architecture*. Everywhere else, the topic is referenced as "native DB primitives — see Data Architecture". Sequence-diagram steps abstracted (`SELECT FOR UPDATE` / `INSERT booking` / `UPDATE filled = true` → "lock vacancy row" / "persist booking" / "mark vacancy filled"); both PNGs regenerated. <br/>**Vocabulary table renames for clarity** — `fee_payment_statuses` → **`judge_fee_entitlements`** (a per-judge attribute answering "is this judge entitled to a fee for their sittings?"); `payment_statuses` → **`payment_lifecycle_statuses`** (state of an individual payment record moving through JFEPS / Liberata). Column renames: `judges.fee_payment_status_id` → `judges.judge_fee_entitlement_id`; `bookings.payment_status` → `bookings.payment_lifecycle_status_id`. PRD FR11/FR33/FR41 wording refreshed to use the clearer concept names. <br/>**Reference-data vocabulary corrected against the docs** — `judge_types` expanded to documented set (CJ, DJ, DJ(MC), DDJ, Recorder, Legal Adviser); `session_types` completed (Full / AM / PM / Evening / Reserved Matter); `absence_types` adds Official Business; `booking_statuses` adds Provisional; `payment_lifecycle_statuses` corrected to (pending / requested / paid / reconciled / queried) per FR41 + data-tables.md; `reconciliation_statuses` corrected to (matched / queried / unreconciled) per data-tables.md; `judge_fee_entitlements` corrected to (yes / no / ask-when-booking) per FR33; `regions` reduced to (Northern, Western) per PRD NFR38; `calendar_periods` expanded to FY 2026/27 + 12 monthly periods. <br/>**HTML build pipeline added** — `scripts/python/build_html.py` (pandoc-based markdown→HTML) + `scripts/build-html.sh` (wrapper). Mirrors the planning-artifacts structure under `html/` at the repo root; rewrites `.md` link targets to `.html`; copies PNG/PDF/DOT/MMD assets; applies a single CSS theme + persistent sidebar navigation; generates an `index.html`. Reads `_bmad-output/planning-artifacts/`; output lives outside `_bmad-output/`. Re-runnable cleanly. <br/>**Mock SQL data files added** — `sql/mock_ref_data.sql` (64 rows across all 15 reference-data tables, every value cross-referenced to the planning docs; `offices` and `ticket_types` are illustrative placeholders, flagged as such in the file header) + `sql/mock_judge_data.sql` (15 rows across the 5 Judge-service tables — Sarah Hawthorne / Emma Patel / Michael Chen threaded across every table). Self-contained DDL + INSERTs; deterministic UUID scheme so FK targets are stable across the two files; `BEGIN / COMMIT` wrapper. | [`../architecture.md` Step 2 *Cross-Cutting Concerns* (retry-safety bullet abstracted); Step 3 *Principle 1* (`vacancies.filled` example abstracted); Step 4 *Data Architecture* (new *Retry safety and concurrency control* subsection — full mechanism table + per-entity convention + Idempotency-Key policy + audit-trail boundary); Step 4 *API & Communication Patterns* (service-to-service retry-safety reference); Step 6 *Cross-Cutting Concerns to File Locations* (retry-safety row abstracted); Step 6 *Requirements to Structure Mapping* (Vacancy & Cover row abstracted); Step 6 *Synchronous call patterns by frequency* (column references abstracted); Step 6 *Data Flow* (R5 step abstracted); Step 3 *Principle 1* (booking's payment-status example now says "payment lifecycle status")](../architecture.md); [`../architecture-summary.md` API patterns table (Idempotency / retry safety row abstracted); Cross-service interaction patterns table (Cross-row workflow safety row abstracted); Foundational principles (Principle 2 native-constructs example abstracted)](../architecture-summary.md); [`./data-tables.md` `judge_fee_entitlements` and `payment_lifecycle_statuses` rows renamed and rewritten with clearer purpose statements; `bookings` table description uses the new `payment_lifecycle_status_id` column name](./data-tables.md); [`./conventions.md` Retry-safety section collapsed to a one-line pointer; DDL example in *Pattern Examples* abstracted; anti-pattern DDL line abstracted; PK naming bullet abstracted; unique-constraint naming bullet abstracted; vocabulary-table list removed (duplicates `./data-tables.md`)](./conventions.md); [`./repository-strategy.md` `ctam-booking` and `ctam-payment` repo descriptions abstracted](./repository-strategy.md); [`./functional-requirements-coverage.md` FR23–FR28 + FR29–FR34 + FR45 architectural-support paragraphs abstracted; FR11 / FR33 / FR41 reworded for new vocabulary](./functional-requirements-coverage.md); [`./repo-structure.md` per-service tree comment abstracted](./repo-structure.md); [`./sequence-diagrams/absence-to-reconciliation.md` Phase 3 narrative + where-to-find link abstracted](./sequence-diagrams/absence-to-reconciliation.md); [`./sequence-diagrams/absence-to-reconciliation.mmd` Mermaid steps abstracted (PNG regenerated)](./sequence-diagrams/absence-to-reconciliation.mmd); [`./sequence-diagrams/payment-batch-flow.md` Phase 2 row abstracted](./sequence-diagrams/payment-batch-flow.md); [`./sequence-diagrams/payment-batch-flow.mmd` Mermaid steps abstracted (PNG regenerated)](./sequence-diagrams/payment-batch-flow.mmd); PRD `FR11` / `FR33` / `FR41` wording updated for new vocabulary; `implementation-readiness-report-2026-05-05.md` historical FR quotes refreshed to match; new files: `sql/mock_ref_data.sql`, `sql/mock_judge_data.sql`, `scripts/python/build_html.py`, `scripts/build-html.sh`, `html/` directory at repo root |
| **v2.8 — DR consolidated as a single open gap; all DR detail removed from elsewhere** | 2026-05-08 | **DR (disaster recovery) is now a single open gap** in [`./gaps.md` G3.6](./gaps.md), holding scope, target region, standby model, geo-redundant backup, AKS DR Helm values, DNS failover, RTO/RPO. The architecture documents previously duplicated this detail in multiple places — all such duplication has been removed and replaced with references to G3.6. <br/>**Specific removals**: dedicated `#### DR topology — UK West cold-standby` subsection in `architecture.md`; the `DR — UK West cold-standby` row in `architecture-summary.md`; the `values-dr-uk-west.yaml` line in `repo-structure.md`; the `(UK West for DR if needed)` note in `starter-template.md`; the "UK West is cold-DR" clause in NFR38 in PRD and the duplicated "UK West DR" clauses in NFR Reliability rows. Section heading *Deployment topology — single Azure region, multi-AZ HA, UK West cold-DR* renamed to *Deployment topology — single Azure region, multi-AZ HA*. <br/>**Gap-content review**: G6.2 mitigation tightened to point at G3.6 for DR rather than repeat the residual-risk handling; the architecture.md *Cross-Component Dependencies* PostgreSQL bullet trimmed to point at the *HA topology* table and the gaps register for residual risks. <br/>**Principle codified**: where a topic is documented as an open gap, the architecture documentation references the gap rather than restating mitigation, scope, or residual-risk detail. Implementing this principle now applies most strongly to DR; future gaps follow the same rule. | [`../architecture.md` Step 2 *Reliability* bullet + Step 4 *Decision Priority Analysis (Important)* + Step 4 *Hosting* + Step 4 *Database hosting* + Step 4 *Deployment topology* (heading renamed; DR sub-section deleted; HA-topology PostgreSQL row updated; full-region-loss footnote rewritten; Helm values list trimmed) + Step 6 *Cross-Component Dependencies* PostgreSQL bullet + Changelog stub](../architecture.md); [`../architecture-summary.md` Deployment / hosting list (DR row + per-service unit Helm-values list)](../architecture-summary.md); [`./gaps.md` G3.6 (consolidated as authoritative DR source) + G6.2 (DR cross-reference tightened) + title bumped to G1–G7](./gaps.md); [`./repo-structure.md` per-service Helm tree](./repo-structure.md); [`./starter-template.md` Default Azure UK region note](./starter-template.md); [`./non-functional-requirements-coverage.md` NFR31 + NFR Reliability architectural support](./non-functional-requirements-coverage.md); PRD `NFR31` data-residency + `NFR38` rollout-isolation + Cloud platform Tech Stack note |
| **v2.7 — Correct + verify all RFC citations; add External References appendix** | 2026-05-08 | **RFC citations corrected**: error envelope now cites **[RFC 9457](https://datatracker.ietf.org/doc/html/rfc9457)** (current; obsoletes [RFC 7807](https://datatracker.ietf.org/doc/html/rfc7807) since July 2023 — content type and field shape unchanged). `Deprecation` header now cited correctly to **[RFC 9745](https://datatracker.ietf.org/doc/html/rfc9745)** (March 2025; was previously incorrectly attributed to RFC 8594). `Sunset` remains [RFC 8594](https://datatracker.ietf.org/doc/html/rfc8594). `Retry-After` header now cited to [RFC 9110 §10.2.3](https://datatracker.ietf.org/doc/html/rfc9110#section-10.2.3); `429 Too Many Requests` status to [RFC 6585](https://datatracker.ietf.org/doc/html/rfc6585). `Deprecation` value corrected — RFC 9745 specifies a date timestamp value (`Deprecation: @<unix-timestamp>`). <br/>**External References appendix** (new): `architecture.md` ends with a table of every RFC / OpenAPI / WCAG cited, each with a verifiable canonical link. <br/>**FR59 / NFR39**: RFC references refreshed; PRD glossary updated. | [`../architecture.md` Step 2 *Cross-Cutting Concerns* + Step 4 *API & Communication Patterns* (RFC citations refreshed) + Step 6 *Cross-Cutting Concerns to File Locations* (problem-details row updated) + Validation Strengths + Implementation Handoff + new *External References* appendix](../architecture.md); [`../architecture-summary.md` API patterns table (Versioning/Error rows updated)](../architecture-summary.md); [`./conventions.md` HTTP headers + Error envelope rows + Process Patterns + Spectral lint reference + anti-patterns](./conventions.md); [`./repo-structure.md` per-service tree (error path updated)](./repo-structure.md); [`./starter-template.md` scaffolding boilerplate](./starter-template.md); [`./gaps.md` G4.5 Spectral ruleset references](./gaps.md); [`./functional-requirements-coverage.md` FR59 reworded](./functional-requirements-coverage.md); [`./non-functional-requirements-coverage.md` NFR39 reworded + architectural support](./non-functional-requirements-coverage.md); PRD `FR59`, `NFR39`, glossary, executive-summary smoke-test bullet |
| **v2.6 — Payment processing reframed as a scheduled batch; service-principal auth restored for the batch only** | 2026-05-07 | **Payment flow**: FR41–FR45 reframed — RSU/Sam confirms bookings/sittings only; the **scheduled batch** (`ctam-payment-batch`) picks up confirmed-but-unpaid records, generates the JFEPS Excel, and dispatches it via Notification → HMCTS Email to the Payment Authoriser. Sam's Journey 1 updated to remove "Sam runs Process Payments." <br/>**Auth (MVP)**: **two patterns** — JWT propagation for user-initiated flows + OAuth `client_credentials` for the batch service principal. v2.5's "no service principals" stance widened back, scoped to the batch case only. <br/>**mock-auth**: re-widened to support `client_credentials`; `mock_oauth_clients` now holds both the SPA client and the `ctam-payment-batch` service-principal registration. <br/>**Sequence diagrams** (new): `architecture/sequence-diagrams/absence-to-reconciliation.md` (user-initiated, 5 phases) + `architecture/sequence-diagrams/payment-batch-flow.md` (batch, 4 phases). <br/>**ASCII flow → links**: Step 6 *Data Flow* in `architecture.md` no longer duplicates the flow as ASCII art — replaced with bullet lists pointing at the two sequence diagrams. <br/>**Gaps**: G1.2 reopened (production service-principal issuer is again MVP-relevant); G7.1 revised — was "post-MVP only", now "pre-Phase-9 decision; default recommendation Azure Workload Identity". <br/>**Assumptions**: A2 reopened, A26 widened back (mock auth must cover both flows), A35 softened (payment batch is the documented exception to "all calls user-initiated"). <br/>**PRD**: NFR12 expanded to cover both auth patterns; FR41–FR45 reframed as batch-driven. | [`../architecture.md` Step 4 *Authentication & Security* (split into JWT propagation + service principals) + Repository List (`ctam-mock-auth` + `ctam-payment` rows updated) + Step 6 *Data Flow* (ASCII replaced with bullet-list + sequence-diagram links)](../architecture.md); [`../architecture-summary.md` auth section + cross-service interaction patterns table + technology stack + "MVP non-user-initiated flow" section](../architecture-summary.md); [`./conventions.md` Communication Patterns: dual auth section](./conventions.md); [`./sequence-diagrams/absence-to-reconciliation.md` (new)](./sequence-diagrams/absence-to-reconciliation.md); [`./sequence-diagrams/payment-batch-flow.md` (new)](./sequence-diagrams/payment-batch-flow.md); [`./gaps.md` G1.2 reopened, G7.1 revised](./gaps.md); [`./assumptions.md` A2 reopened, A26 widened, A35 softened](./assumptions.md); [`./data-tables.md` mock_oauth_clients widened to cover service principals](./data-tables.md); PRD `FR41`–`FR45`, `NFR12`, Sam's Journey 1; both implementation-readiness reports |
| **v2.5 — Lock MVP auth model: HMCTS IdP for human users only; JWT propagation for all inter-service auth; no service principals at MVP** | 2026-05-07 | **Auth (MVP)**: HMCTS IdP for human OIDC `authorization_code` only — no service principals, no `client_credentials`, no mTLS. <br/>**Inter-service**: JWT propagation — forward user's JWT via per-service `RestClient` interceptor. <br/>**mock-auth**: narrowed to `authorization_code` flow only; `mock_oauth_clients` holds only the SPA client registration. <br/>**Data seeding**: dev/CI via one-off scripts; production via operator-initiated ETL (post-MVP refinement). <br/>**Gaps**: G1.2 closes; G4.7 (operator-ETL post-MVP refinement) + G7 (service-identity question for any future non-user-initiated flow) added. <br/>**Assumptions**: A2 retires; A26 narrows; A35 added ("all MVP runtime calls user-initiated"). | [`../architecture.md` Step 4 Authentication & Security, Phasing of Authentication, Repository List, Implementation Sequence, External Integration Points, Decision Priority Analysis, TBDs Resolved, Coherence Validation, Architecture Strengths](../architecture.md); [`../architecture-summary.md`](../architecture-summary.md); [`./conventions.md` Communication Patterns / JWT propagation](./conventions.md); `./diagrams/system-context.dot`; [`./gaps.md` G1.2 closure, G4.7, G7](./gaps.md); [`./assumptions.md` A2 retire, A26 narrow, A35 new](./assumptions.md); [`./data-tables.md` mock_oauth_clients narrowing](./data-tables.md); PRD `FR5` / `NFR12`; both implementation-readiness reports |
| **v2.4 — Show user → HMCTS IdP authentication on the system context diagram** | 2026-05-07 | **Diagram**: explicit `users → idp` SSO edge added; `users → ui` label simplified to "HTTPS + JWT". <br/>**Auth flow**: now reads top-to-bottom — user authenticates at IdP → JWT → UI/APIM → service `JWTFilter` validates against JWKS. <br/>**Summary doc**: opens auth section with a 3-step numbered flow; *Cross-service interaction patterns* table gains a *User authentication* row. | `./diagrams/system-context.dot`; [`../architecture-summary.md`](../architecture-summary.md) |
| **v2.3 — Make HMCTS IdP integration with CTAM Pathfinder services explicit (JWKS validation + service tokens)** | 2026-05-07 | **Diagram**: new cluster-level `Cross-cutting → HMCTS IdP` edge for JWKS-based JWT validation. <br/>**architecture.md**: `JWTFilter` step 1 specifies JWKS fetch; *Integration Points — External* HMCTS IdP row reworded to name JWKS + per-service validation pattern. <br/>**Summary doc**: auth section expanded to make JWKS validation explicit; *Cross-service interaction patterns* table fixed (User request row no longer attributes JWT validation to APIM). | [`../architecture.md` Step 4 *Authentication & Security*, Step 6 *Integration Points — External*](../architecture.md); [`../architecture-summary.md`](../architecture-summary.md); `./diagrams/system-context.dot` |
| **v2.2 — Drop dedicated `ctam-configuration` service; demote `configuration_values` to a shared infrastructure table** | 2026-05-07 | **Decomposition**: 12 → 11 services (`ctam-configuration` dropped). <br/>**Repos**: 15 → 14. <br/>**Configuration**: per-service via Spring profiles + Key Vault; cross-service via shared `configuration_values` infrastructure table (no API). <br/>**Tables**: `configuration_values` moves to *Shared infrastructure tables*; total stays 37 (34 service-owned production + 1 shared infrastructure + 2 dev-only). | [`../architecture.md` Step 1, Step 2, Step 3, Step 4 Implementation Sequence, Step 6 Repository List, Step 7 coverage tables, Step 8 Implementation Handoff](../architecture.md); [`./data-tables.md`](./data-tables.md); `./diagrams/system-context.dot`; PRD `D1` / `FR8` / "All 11 services live" / "Cross-cutting services" / "Phase 0 Foundations"; brainstorming-1600.md `D1` / Phase 0 scope / migration table; both implementation-readiness reports |
| **v2.1 — Drop custom idempotency-key tables; use DB-native retry-safety primitives** | 2026-05-06 | **Tables**: `booking_idempotency_keys` + `payment_idempotency_keys` dropped (39 → 37). <br/>**Retry safety**: PostgreSQL natural-key unique constraints (`uq_*`) → 409; JPA `@Version` optimistic locking → 412; `SELECT … FOR UPDATE` pessimistic row locks for cross-row workflows. <br/>**Boilerplate**: per-service `IdempotencyFilter` retired. <br/>**Design rule**: prefer native platform constructs over custom entities (captured to user-feedback memory). | [`../architecture.md` Step 2 Cross-Cutting Concerns + Step 3 "what is duplicated" + Step 4 Authentication & Security idempotency line + Step 4 cross-service access table + Step 6 Cross-Cutting Concerns to File Locations + Repository List `ctam-booking` / `ctam-payment` rows + Step 5 stub intro + Step 7 Coherence Validation / Requirements Coverage / Completeness Checklist](../architecture.md); [`./conventions.md` Naming Patterns service-prefix examples + Constants examples + Communication Patterns retry-safety section](./conventions.md); [`./data-tables.md` Booking + Payment + Inventory totals + retry-safety convention note](./data-tables.md); [`./repo-structure.md` per-service `config/` directory](./repo-structure.md) |
| **v2.0 — Same-Azure-region multi-AZ HA topology (retracts implied per-Azure-region active/active)** | 2026-05-06 | **Deployment**: single Azure region (UK South) with multi-AZ HA — AKS multi-zone node pools, PostgreSQL zone-redundant HA (sync replication, automatic failover), APIM/Key Vault/ACR zone-redundant. <br/>**DR**: UK West cold-standby (post-MVP scope decision per G3.6). <br/>**Region disambiguation**: *Azure region* (geographic) vs *HMCTS judicial region* (business — FR58 activation flag) are different things. <br/>**Helm values**: per-environment (`values-production.yaml`) instead of per-Azure-region. <br/>**Retracted**: v1.x "per-region AKS clusters" wording. <br/>**Gaps/Assumptions**: A34 (UK South 3 AZs) + G3.6 (UK West DR scope) added; G6.2 mitigation refined. | [`../architecture.md` Step 1 Reliability bullet, Step 4 Hosting + Database hosting + Decision Priority Analysis (HA topology row) + Region Isolation rewrite + Cross-Component Dependencies, Step 7 Coherence Validation Structure Alignment + NFR Reliability coverage + Architecture Strengths](../architecture.md); [`./repo-structure.md` per-service `helm/` directory](./repo-structure.md); [`./assumptions.md` A34](./assumptions.md); [`./gaps.md` G3.6 + G6.2](./gaps.md); PRD `NFR38` |
| **v1.9 — Phase 0 Data Migration ETL framing (separated from Flyway DDL; CTAM Pathfinder tables decoupled from APEX shape)** | 2026-05-06 | **Migration framing**: CTAM Pathfinder tables are CTAM Pathfinder's own design; APEX schema is the *data source*, not the target shape. <br/>**Flyway**: CTAM Pathfinder DDL only (no APEX-data ingestion via Flyway). <br/>**Phase 0 ETL**: `ctam-architecture/migration/` reads APEX dumps → transforms → loads via CTAM Pathfinder Reference Data + Authorisation APIs. <br/>**Repo structure**: `ctam-architecture/migration/` subdirectory documented with rationale "why migration lives here, not in services or Flyway". <br/>**Gaps/Assumptions**: G4.6, A29, A32, A33 reframed for the ETL/Flyway separation. | [`../architecture.md` Step 4 Data Architecture, Step 4 Implementation Sequence step 4 + step 7, Repository List, Implementation Handoff step 4](../architecture.md); [`./data-tables.md`](./data-tables.md); [`./gaps.md` G4.6](./gaps.md); [`./assumptions.md` A29, A32, A33](./assumptions.md); [`./repo-structure.md` `ctam-architecture` repo + new "Why migration lives here" rationale](./repo-structure.md); PRD `FR57`, `D3`, `D9`, "What Makes This Special" #4, Phase 0 migration correctness bullet; brainstorming-session-2026-05-05-1600.md `D3`, `D9`; implementation-readiness-report-2026-05-05.md `FR57` |
| **v1.8 — Strategy B refactor (architecture vs implementation split)** | 2026-05-06 | **Refactor**: `architecture.md` (~2,050 lines) split into an index + 7 siblings under `architecture/`. <br/>**Sibling files created**: `gaps.md`, `assumptions.md`, `changelog.md`, `conventions.md`, `repo-structure.md`, `starter-template.md`, `data-tables.md`. <br/>**Anchors**: pre-v1.8 anchors redirected via the header table in `changelog.md`. | All — sibling files created in [`architecture/`](.); main `architecture.md` stubs replaced moved sections with one-line links into the siblings |
| **v1.7 — Manual UAT replaces automated APEX-comparison parity tests** | 2026-05-06 | **Behavioural parity**: D5 / FR61 / NFR41 reframed — manual UAT by APEX-experienced users replaces automated APEX-as-oracle harness. <br/>**Removed**: `@ApexOracle` test base class; `*ApexParityTest.java`; per-service `parity/` directory; APEX-during-build HTTP scraping. <br/>**Added**: per-service `docs/uat/` directory for manual UAT walkthrough scripts. <br/>**Cross-doc**: brainstorming-1600 D5 + Risks; PRD `FR61`/`NFR41`/Decisions Log D5; both implementation-readiness reports. | Step 2 — Cross-Cutting Concerns; [`./starter-template.md` Per-service CTAM Pathfinder Conventions, Foundational Principles "What is duplicated"](./starter-template.md); Step 4 — Phasing of Authentication, Implementation Sequence; [`./conventions.md` Test conventions, Naming Patterns](./conventions.md); [`./repo-structure.md` Per-service tree, Project Directory Structure](./repo-structure.md); Step 6 — External System Boundaries, Cross-Cutting Concerns to File Locations, External Integration Points, Region rollout flow; Step 7 — Coherence Validation Pattern Consistency, NFR Maintainability coverage, [`./assumptions.md` A12, A13](./assumptions.md), [`./gaps.md` G5.1](./gaps.md), Architecture Strengths, Implementation Handoff; PRD `FR61` / `NFR41` / Decisions Log D5; brainstorming `D5` / Risk #11 / Risk #12 / captured idea #4; both implementation-readiness reports |
| **v1.6 — Consistency review + vocabulary inventory** | 2026-05-06 | **Consistency**: 31+ contradictions resolved across the document. <br/>**Booking ↔ Vacancy**: `markFilled` API endpoint removed; Booking writes `vacancies.filled` via direct DB UPDATE in-transaction (PRD `FR30` updated). <br/>**Reference Data**: reads via direct SQL; writes via API. <br/>**Region isolation**: app tier per-region; DB tier shared globally (HMCTS best-practice alignment noted as post-MVP review). <br/>**Vocabularies**: 12 vocabulary tables (table-per-vocabulary, Option B). <br/>**Data inventory**: new Authoritative Table Ownership Mapping (39 entities; provisional pending APEX SQL dump validation). | Step 2 — Cross-Cutting Concerns; [`./starter-template.md`](./starter-template.md); Step 4 — Data Architecture, Authentication, Infrastructure & Deployment + [`./data-tables.md`](./data-tables.md); [`./conventions.md` Naming Patterns, Communication Patterns](./conventions.md); Step 6 — Boundaries, Data Flow, Integration Points; Step 7 — Coherence Validation, FR/NFR Coverage tables; PRD `FR30` updated for cross-document consistency |
| **v1.5 — Hybrid DB topology** | 2026-05-06 | **Schema**: single shared schema (no schema-per-service); table-name convention (entity-plural for primary, service-prefix for ambiguous). <br/>**DB roles**: per-service roles retained as forward-compatibility hooks; broad Day-1 grants tightening over time. <br/>**Enforcement**: ArchUnit fitness functions verify table ownership. <br/>**Gaps**: G6.6 (table-name collision risk) added. | Step 3 — Principle 1; Step 4 — Data Architecture; [`./conventions.md` Database naming](./conventions.md); Step 6 — Service & Data Boundaries; [`./gaps.md` G6.6](./gaps.md); [`./assumptions.md` A31](./assumptions.md) |
| **v1.4 — HMCTS Crime template alignment** | 2026-05-06 | **Schema migrations**: Liquibase → Flyway. <br/>**Build**: Gradle Kotlin DSL → Groovy DSL. <br/>**OpenAPI**: springdoc-openapi → Swagger Core + Maven-published API spec artefacts. <br/>**Observability**: Application Insights direct ingestion → OpenTelemetry → App Insights. <br/>**Authentication**: Spring Security filter chain → custom `JWTFilter` (HMCTS template pattern; calls Authorisation per request, required by FR58). <br/>**Added libraries**: Lombok, MapStruct, OWASP encoder, JaCoCo, CycloneDX, gradle-git-properties. <br/>**Gaps**: G1.4 confirmed via template review; G1.4a (Helm not in template), G1.4b (Key Vault not in template) added. | [`./starter-template.md`](./starter-template.md); Step 4 — Authentication, API & Communication, Infrastructure & Deployment; [`./conventions.md`](./conventions.md); [`./repo-structure.md` per-service tree](./repo-structure.md); [`./gaps.md` G1.4–G1.4b](./gaps.md); [`./assumptions.md` A4, A29, A32](./assumptions.md) |
| **v1.3 — Shared DB + Principle 2** | 2026-05-06 | **DB**: single global PostgreSQL (replaces per-service DBs). *(Schema-per-service introduced here was superseded in v1.5.)* <br/>**Foundational principles**: split into two — (1) API for workflows + shared DB for simple data access; (2) No premature optimization. <br/>**Read federation**: Strategy A federation retired → SQL JOINs over shared schema. <br/>**Caching**: Caffeine cache dropped (direct DB reads). <br/>**Risks**: Risk #9 (Strategy A NFR breach) retires. <br/>**Gaps/Assumptions**: G6 series + A28–A32 added. | Step 3 — Foundational Principles; Step 4 — Data Architecture; [`./gaps.md` G6](./gaps.md); [`./assumptions.md` A28–A32](./assumptions.md) |
| **v1.2 — Documented gaps + assumptions** | 2026-05-06 | **Verdict**: refined to **READY WITH DOCUMENTED GAPS**. <br/>**Registers**: G1–G5 (Documented Gaps) and A1–A27 (Assumptions) elevated to authoritative sections so implementation can begin while gaps are tracked in parallel. | [`./gaps.md`](./gaps.md); [`./assumptions.md`](./assumptions.md); Step 7 Architecture Readiness Assessment |
| **v1.1 — Mock-first authentication** | 2026-05-06 | **Mock auth**: `ctam-mock-auth` covers Phase 0–8 dev/CI/integration. <br/>**Real IdP**: HMCTS IdP integration deferred to a discrete pre-Phase-9 cutover. <br/>**Gaps**: G1.x (HMCTS IdP) reclassified from Phase 0 blockers to pre-Phase-9 prerequisites. | Step 4 — Phasing of Authentication; Step 4 Implementation Sequence; Step 6 Repository List; Step 7 G1, G5, A26, A27 (now in [`./gaps.md`](./gaps.md), [`./assumptions.md`](./assumptions.md)) |
| **v1.0 — Initial** | 2026-05-06 | **Initial 8-step architecture** aligned with PRD locked decisions D1–D9. <br/>**Stack**: Java 25 + Spring Boot 4 + Gradle Kotlin DSL + Liquibase + per-service PostgreSQL + Strategy A federation + Caffeine cache + Spring Security filter chain + springdoc-openapi + schema-per-service. <br/>**Foundational principle**: API as only boundary; no shared library. <br/>**Verdict**: READY FOR IMPLEMENTATION. | All |
